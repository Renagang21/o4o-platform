# NPM Configuration
# Development environment stderr redirection issue prevention
loglevel=warn
progress=false

# Prevent npm "2" append bug (origin unknown)
save-exact=true
package-lock=true
audit-level=moderate

# Resolve peer dependency conflicts
legacy-peer-deps=true
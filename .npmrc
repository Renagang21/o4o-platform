# NPM Configuration
# Firebase Studio 환경에서 stderr 리다이렉션 문제 방지
loglevel=warn
progress=false

# Prevent Firebase Studio npm "2" bug
save-exact=true
package-lock=true
audit-level=moderate

# Resolve peer dependency conflicts
legacy-peer-deps=true
# WO-O4O-HFF-EN-C91-FUNCTIONAL-CLAIM-LOSS-4802-FULL-REPAIR-V1 — CHECK

HFF 영어 canonical 의 **공식 기능성 누락** 복원. census 최우선 클러스터 `C91`.

| 항목 | 값 |
|------|----|
| 착수 HEAD | `340e24f4c` (조상 확인 **true**) |
| 브랜치 / 작업트리 | `main` / 내 파일 clean (타 세션 WIP 별도) |
| DB | 프로덕션 `o4o_platform` (Cloud SQL Auth Proxy `:5541`) |
| 판정 | **PASS** — 검증 게이트 전량 통과 · `expected == actual` · 독립검증 일치 |

---

## 1. 대상 재현 (§2·§3) — 오탐 3종 제거

census 가 보고한 4,802 는 개수 차이 기반 추정치였다. §3 이 요구한 대로 **단순 개수 차이로 판정하지 않고**
KO 기능성 나열을 항목으로 분해해 각 항목이 EN 에 의미상 대응되는지 **개념 토큰**으로 확인했다.

| 단계 | 대상 문서 | 제거된 오탐 |
|---|---:|---|
| 초안(개수 차이) | 8,534 | — |
| ① 기능성 문장만 대상 | 3,063 | 주의사항 문장을 `,` 때문에 나열로 오인 (`특정질환, 특이체질, 알레르기체질, 임산부…` 314건 등) |
| ② 하이픈 표기 수용 | 2,956→ | `body-fat`·`blood-flow` 등 하이픈형 미매칭 (118건 등) |
| ③ 개념 사전 보강 | **2,956** | `구강에서의 항균작용` ↔ `in the mouth` 미매칭 (107건) |

**최종 실제 누락: 2,956 문서 / 고유 패턴 446 / 누락 항목 7,889개**
오탐으로 판정해 제외: 37,670 문서.

> 판정은 보수적으로 했다 — 개념 사전에 없는 항목은 `missing` 이 아니라 `unknown` 으로 분류해
> **교정 대상에 넣지 않았다**. 근거 없는 수정을 하지 않기 위해서다.

### 1-1. 대표 누락 패턴

| 문서 | KO | EN(누락 상태) | 누락 항목 |
|---:|---|---|---|
| **922** | `면역력 증진·피로개선·혈소판 응집억제를 통한 혈액흐름·기억력 개선·항산화에 도움을 줄 수 있음` | `May help improve blood circulation by inhibiting platelet aggregation.` | 면역력·피로·기억력·항산화 **4개** |
| 124 | 위와 동일(홍삼 접두) | 동일 | 4개 |
| 114 | `기억력 개선·혈행 개선` | `May help improve memory.` | 혈행 개선 |
| 76 | `혈중 중성지질 개선·혈행 개선` | `May help improve blood triglycerides.` | 혈행 개선 |
| 68 | `EPA 및 DHA … 혈중 중성지질·혈행·건조한 눈` | `…improve blood triglycerides, improve blood circulation.` | 건조한 눈 |

상위 10 클러스터가 전체의 49.4%, 상위 100 이 84.6% 를 설명한다(§5 공통 패턴 우선 원칙 충족).

## 2. 복원 방법 (§4·§5)

**KO canonical 이 기준이고 EN 은 근거로 쓰지 않았다.** 다만 표현은 새로 지어내지 않고
**코퍼스에서 수확**했다 — 같은 KO 항목이 다른 문서에서 이미 올바르게 번역돼 있으므로 그 EN 절을 재사용한다.

1. 단일 기능성 문장(KO 항목 1개)이 정상 번역된 쌍에서 EN 절을 수확 → **2,109개 절**
2. `May help …` / `Needed …` 앞머리를 제거해 절만 남김(조립 시 한 번만 사용)
3. 누락 항목을 수확 절로 치환해 **기존 EN 문장 뒤에 `;` 로 이어 붙임**
   — 기존 표현은 건드리지 않고 **빠진 것만 추가**한다(§4)
4. 첫 항목에 원료명이 붙은 경우(`홍삼 면역력 증진`) 왼쪽 토큰을 하나씩 떼며 재조회

| 항목 | 값 |
|------|----|
| 수확한 EN 절 | 2,109 |
| 교정 계획 수립 문서 | **2,769** |
| 계획 미수립 | 187 (수확분에 없는 KO 항목 36종) |

복원 예:

```
before: May help improve blood circulation by inhibiting platelet aggregation.
after : May help improve blood circulation by inhibiting platelet aggregation;
        boosting immunity; reducing fatigue; improving memory; antioxidant activity.
```

## 3. 검증 (§6) — 적용 전 전량 통과

계획 2,769건을 실제 DB 본문에 적용해 보고 게이트를 통과한 것만 대상으로 삼았다.

| 게이트 | 위반 |
|------|-----:|
| 수치·단위 drift | **0** |
| HTML 구조(li·h2·sd-item·sd-tag·b·ul·p) | **0** |
| 한글 증가 | **0** |
| 영어 위생(`;;` · `..` · 단어중복 · 단어붙음 · `;.`) | **0** |
| 절이 실제로 추가되지 않음 | **0** |
| EN 본문 drift(낙관적 잠금) | **0** |
| **슬롯 치환 실패** | **915** → 대상 제외 |

**최종 적용 대상: 1,854 문서**

### 3-1. 슬롯 치환 방식 교정 (기록)

초안은 정규화된 슬롯 텍스트로 literal replace 를 했는데 **920건이 실패**했다. 슬롯 안에 인라인
태그(`<b>`)가 있으면 정규화 텍스트가 원문과 일치하지 않기 때문이다. 슬롯을 **인덱스와 오프셋으로
특정해 치환**하도록 바꿨고(뒤에서부터 치환해 오프셋 밀림 방지), 인라인 태그가 있는 슬롯은
**텍스트만 안전하게 갈아끼울 수 없으므로 대상에서 제외**했다(915건). 근거 없는 구조 변형을 하지 않기 위해서다.

## 4. Apply (§7) — expected == actual

`data/hff-en-c91-apply-result-v1.json` — **UPDATE 전용**, 이중 게이트(`--apply` + `HFF_EN_C91_APPLY_CONFIRM=YES`),
rollback manifest(교정 전 본문 전량) 선기록, 행 단위 **본문 해시 낙관적 잠금**, 샤드 트랜잭션.

| 항목 | expected | actual |
|------|---------:|-------:|
| UPDATE | 1,854 | **1,854** |
| INSERT | 0 | **0** |
| SKIP | 0 | **0** |
| 실패 샤드 | 0 | 0 |

| 전역 | before | after |
|------|-------:|------:|
| KO canonical | 40,918 | 40,918 |
| **EN canonical** | 40,902 | **40,902** (총수 불변 — UPDATE 전용) |
| JA canonical | 32,270 | 32,270 |
| ZH canonical | 40,918 | 40,918 |
| ProductMaster(HFF) | 40,948 | 40,948 |

## 5. 독립검증 (§8)

Apply 산출물을 신뢰하지 않고 **DB 를 다시 읽어 전체 EN 40,902 에 기능성 census 를 재실행**했다.

| 항목 | before | after |
|------|-------:|------:|
| 기능성 누락 문서 | 2,956 | **1,102** |
| 고유 누락 패턴 | 446 | 276 |
| 누락 항목 총수 | 7,889 | 2,431 |
| **해소 문서** | — | **1,854** |

해소 수 **1,854 = Apply UPDATE 수**로 정확히 일치한다. 새로운 회귀(기능성 추가·병합·원료 귀속 drift)는
발생하지 않았다 — 재census 는 교정된 문서를 `NO_REAL_LOSS` 로 재분류했고 나머지 지표는 변동 없다.

| 항목 | 값 |
|------|----|
| KO / JA / ZH / ProductMaster 불변 | 예 / 예 / 예 / 예 |
| EN canonical 총수 불변 | 예 (40,902) |
| canonicalDup | 0 |
| 대상 밖 EN write | 0 (UPDATE 대상은 `updatedIds` 1,854 뿐) |

## 6. 잔여 1,102 문서 — 사유별

| 사유 | 문서 | 다음 처리 |
|------|-----:|---|
| 슬롯에 인라인 태그가 있어 텍스트 치환 불가 | 915 | 슬롯 재조립 방식(HTML 구조 보존 치환) 도입 후 재실행 |
| 수확 절이 없는 KO 항목 36종 | 187 | 해당 항목 EN 절을 직접 저작 후 재실행 (`오메가3 EPA 및 DHA 함유 유지 : 혈중 중성지질 개선` 103건 등) |

**최종 예외 0** — 두 사유 모두 방법이 확정돼 있어 다음 사이클에서 해소 가능하다.
개별 특이 문서를 문제 큐로 보낸 건은 없다(§9).

## 7. 중지 조건 (§9) — 해당 없음

모집단 재현 성공 · KO↔EN 원료 귀속 일관 · 대량 기능성 추가/누락 없음 · 대상 밖 write 0 ·
중복 canonical 0 · rollback 가능 · 독립검증 통과.

## 8. 산출물

| 파일 | 내용 |
|------|------|
| `hff-en-c91-survey.mjs` | 대상 재현 + 오탐 분리(개념 토큰 대조) |
| `hff-en-c91-repair.mjs` | 코퍼스 절 수확 + 복원문 조립 |
| `hff-en-c91-apply.mjs` | 검증 게이트 + UPDATE 전용 apply |
| `data/hff-en-c91-survey-v1.json` | 대상·클러스터 실측 |
| `data/hff-en-c91-repair-plan-v1.json` | 복원 계획·수확 통계·미해소 항목 |
| `data/hff-en-c91-rollback-v1.json` | 교정 전 본문 전량 (1,854행) |
| `data/hff-en-c91-apply-result-v1.json` | apply 결과 + `updatedIds` |

## 9. 다음 repair cluster 권장

C91 이 부분 완료(1,854/2,956)됐으므로 다음은 census 순서대로:

1. **C01 `Labelled standard` 한글 잔존 40,896** — 최대 규모
2. **C02 전문가 안내 25,404** — 고정 문장 1개로 해소
3. C90 단어 붙음 58,734 인스턴스(자동 교정)
4. C91 잔여 1,102 (슬롯 재조립 + 절 36종 저작)
5. C13 수치 1,010 → C14 인정번호 77 → C16 구조 452 → C92 용어

## 10. 계약 준수

| 항목 | 결과 |
|------|:----:|
| KO·ZH·JA canonical / ProductMaster / candidate 수정 | **없음** |
| INSERT | **0** (UPDATE 전용) |
| EN 미보유 16건 생산 | **없음** (§7 준수) |
| 기존 EN 표현 불필요한 재작성 | **없음** (빠진 절만 추가) |
| 타 세션 WIP · `pnpm-lock.yaml` 접촉 | **없음** |
| rollback 가능 | 예 — `hff-en-c91-rollback-v1.json` 1,854행 원본 |

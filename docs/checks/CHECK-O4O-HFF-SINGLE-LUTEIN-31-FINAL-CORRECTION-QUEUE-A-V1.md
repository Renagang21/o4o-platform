# CHECK — single-lutein 31 최종 교정 큐 확정 (Agent A) V1

- 상위 WO: `WO-O4O-HFF-MX-MULTI-INGREDIENT-EXPANSION-PILOT-V1` / 흡수교정 트랙
- 선행: 파서 하드닝 `6a2769045` (Agent B) · 설계 `fa082f7f8` (Agent A) · blocker `ee10f5375` (Agent B)
- 성격: **read-only · DB write 0 · canonical 변경 0 · apply 미실행**
- 기준선: 복합형 LIVE **605**
- 산출물: `docs/checks/data/product-description-guard/hff-lut31-final-correction-queue.json` · 독립 감사기 `apps/api-server/src/scripts/hff-lut31-independent-audit.cjs`

---

## 1. 검수 방법 — 공용 파서 **미사용** 독립 판정

31건의 `BASE_STANDARD` · `MAIN_FNCTN` **원문을 DB에서 직접 조회**(read-only)하고, `hff-source-parse` 를 재사용하지 않는 **독립 구현**으로 spec·귀속을 재판정한 뒤 파서 산출(`hff-lut31-rederived.json`)과 1:1 대조했다. 불일치는 전건 원문으로 adjudicate 했다(중지조건 준수).

### 적용한 비타민E 정책 (지시)
| 조건 | 처리 |
|---|---|
| BASE 표시량 spec 존재 **∧** MAIN_FNCTN 에 비타민E 로 **명시 귀속**된 항산화 기능성 | verifiedFullSet **포함** |
| spec 만 존재, 기능성 직접 귀속 없음 | **부원료 제외** |
| 귀속 또는 spec 모호 | **REVIEW/HOLD** |

**실측: `spec만 존재(귀속 없음)` 케이스 0건.** 비타민E 보유 23건은 전부 spec + 명시 귀속을 동시 충족 → 정책상 전건 포함.

## 2. 파서 대비 불일치 5건 — 전건 원문으로 해소 (파서 gap 2종)

| gap | 건수 | 근거 | 판정 |
|---|:-:|---|---|
| **`UNIT_MODIFIER_UG_RAE`** | 3 | `hff-source-parse.ts:51` 의 `s.replace(/(?<=[\d\s(])u\s*g\b/gi,'μg')` 가 **`\b` 때문에 `700ugRAE`·`210 ugRE` 처럼 수식어가 결합되면 정규화 실패** → 단위 미매치로 **spec 전체 누락**. 원문에는 `비타민A : 표시량(700ugRAE/300mg)의 80~150%` 가 명백히 존재 | **원문 우선 — 파서 under-extraction** |
| **`FN_MODE_COLON`** | 2 | MAIN_FNCTN 이 `비타민E : 항산화…` / `마리골드꽃추출물 : 노화로…` 형태의 **콜론 라벨 명시 구조**인데 파서가 bracket/numbered 만 인정 → `inline` 폴백. 그 결과 항산화 문구가 **구리·망간·비타민C·셀레늄에 registry 추정 귀속**(원문에 해당 원료 없음) | **원문 우선 — 파서 오귀속** |

- 두 gap 모두 **원문에 근거가 명확**하여 수기 판정이 우선한다. 나머지 26건은 spec·mode·E판정 전부 파서와 일치.
- ⚠️ `UNIT_MODIFIER_UG_RAE` 는 **생산 `hff-combo-select` 가 쓰는 공용 spec 파서와 동일 경로** → 다른 그룹에서도 후보 **과소추출** 가능(누락 방향이라 기존 LIVE 콘텐츠 오염은 아님). 별도 재점검 권고.
- 참고: 루테인은 BASE spec 라벨(`루테인`)과 기능성 라벨(`마리골드꽃추출물`)이 상이 — 독립 감사기에도 동의어 반영(원문 라벨 전수 확인: 마리골드꽃추출물 21 · 루테인 제품 6 · 루테인지아잔틴… 2 · 콜론형 2).

## 3. 최종 판정 (원문 기준)

| 판정 | 건수 |
|---|:-:|
| **PASS** | **31** |
| REVIEW | **0** |
| KEEP_SINGLE | **0** |

- 모든 PASS 는 verifiedFullSet 의 **각 원료가 BASE spec ∧ MAIN_FNCTN 명시 귀속을 동시 충족**(양쪽 근거 없는 PASS 0).
- **단일 루테인으로 남는 제품 0건** — "single-lutein" 31 은 전량 다원료 제품이며 **전건 RETIRE+REPLACE 대상**이다.

### 최종 그룹별 수량
| 그룹 | 건수 |
|---|:-:|
| 루테인+비타민A+비타민E | **21** |
| 루테인+비타민A | **8** |
| 루테인+비타민E | **2** |
| 단일 루테인 유지 | **0** |
| REVIEW | **0** |

## 4. 기존 큐(fa082f7f8) 31행 대비 변경 — **15건**

| 변경 | 건수 |
|---|:-:|
| `루테인+비타민E` → `루테인+비타민A+비타민E` | 13 |
| `루테인+비타민A` → `루테인+비타민A+비타민E` | 2 |
| 변경 없음 | 16 |

- 큐의 2원료 전제가 다수 무너짐 → **blocker `ee10f5375` 의 "큐 10/15/6 불성립" 재확인**. 확정 분포는 21/8/2.
- 고N 혼입 가드: 큐의 루테인 관련 행 79 중 **본 31건만** 대상(48행은 오메가3·B군 등 고N — 미포함, 검수 범위 밖).

## 5. B_BENIGN 정의 정합성 — **수정안 제시**

현행(설계 §2): *"부가=비타민E뿐 · MAIN_FNCTN E 기능성 **없음** → 항산화 부원료 → KEEP_SINGLE"* (23건).

- **원칙은 새 정책과 정합**한다("기능성 귀속 없음 → 부원료 제외"와 동일).
- 다만 본 감사에서 **판정 근거 규정의 공백**이 실측으로 드러났다. 아래를 명문화해야 오판을 막는다.

```text
B_BENIGN (개정안)
정의: 부가 원료가 비타민E 단독이고, MAIN_FNCTN 에 비타민E 로 **명시 귀속된** 기능성이 없는 경우
      → 항산화 부원료로 full-set 제외, KEEP_SINGLE.

판정 규칙(신설)
 (a) 귀속 판정은 **명시 라벨 구조만** 사용한다. registry 인라인 매칭에 의한 추정 귀속 금지.
     — 실증: 항산화 문구가 원문에 없는 구리·망간·비타민C·셀레늄으로 추정 귀속됨(20040020006460).
 (b) 명시 구조 = ① [원료] 브래킷 ② n) 원료 : ③ **원료 : (콜론 라벨)** — 3형식.
     — ③ 미인정 시 콜론형 제품이 "귀속 없음"으로 오분류되어 benign 오배정 위험.
 (c) spec 집합 산출 시 **단위 수식어 결합형**(ugRAE·ugRE·μgRE) 정규화 필수.
     — 미정규화 시 "부가=비타민E뿐" 전제 자체가 오판(실제로는 비타민A 동반).
 (d) (a)~(c) 로 판정 불가하면 benign 자동 배정 금지 → REVIEW.
```

- **본 31건에 benign 해당 0건**(E 기능성이 전부 명시 귀속). 설계의 lutein benign 2건은 본 31 범위 밖.

## 6. Agent B 최종 교정 대상 (statementNo)

전 31건 `action = RETIRE_REPLACE`. 제품별 근거는 산출 JSON(`vitaminE.specEvidence` / `fnEvidence` / `attributionMode`)에 포함.

### 6-1. 즉시 처리 가능 — **26건** (파서 산출과 일치)
```text
루테인+비타민A+비타민E (19):
200400170061613 20190004553406 20040020006920 20220020002114 200400200141549
200400200141406 20040015083571 200400170061602 20070017035374 200600200081366
20190004553405 20040017006644 20040017006974 200400200023677 20040017006722
200400170061390 20040017021708 2021001285525  200400200023870
루테인+비타민A (6):
20120019007565 200400200022869 200400200061059 20040015083657 20040015083520
200700170351475
루테인+비타민E (1):
20070017035608
```
> 위 statementNo 목록은 산출 JSON 의 `agentBReady=true` 26건이다. **정본은 JSON**(`hff-lut31-final-correction-queue.json`) — 실행 시 JSON 을 소스로 사용할 것.

### 6-2. 파서 수정 선행 필요 — **5건**
| statementNo | 제품 | verifiedFullSet | 필요 수정 |
|---|---|---|---|
| 201200190071247 | 루테인 | 루테인+비타민A+비타민E | `UNIT_MODIFIER_UG_RAE` |
| 200600200081422 | 아이케어 루테인 지아잔틴 미니 | 루테인+비타민A+비타민E | `UNIT_MODIFIER_UG_RAE` |
| 200400170061351 | 필리 루테인 | 루테인+비타민A | `UNIT_MODIFIER_UG_RAE` |
| 20040020006460 | 아이시안루테인 | 루테인+비타민E | `FN_MODE_COLON` |
| 20120019007396 | 아이파워루테인포르테 | 루테인+비타민A | `FN_MODE_COLON` |

## 7. 파일럿 재개 가능 여부

- **가능 — 단, 파서 2건 수정 선행을 권고**.
  - 권고안: `hff-source-parse.ts` ① `ug→μg` 정규화의 `\b` 제거(수식어 결합 허용) ② 귀속 파서에 **콜론 라벨 모드** 추가 → 재산출 시 31/31 이 본 큐와 일치해야 함(합치 게이트).
  - 수정 없이 진행하려면 **6-1의 26건만** 대상으로 하고, 5건은 보류한다(파서가 잘못된 fullSet 을 산출하므로 그대로 생산 시 오콘텐츠).
- apply 구현·DB 작업은 본 큐 확정 + 이중게이트 승인 후.

## 8. 보고 요약

```text
PASS 31 / REVIEW 0 / KEEP_SINGLE 0
최종 그룹: 루테인+비타민A+비타민E 21 · 루테인+비타민A 8 · 루테인+비타민E 2 · 단일유지 0
기존 큐 대비 변경: 15건 (2원료→3원료 15) · 무변경 16
파서 대비 불일치: 5건 → 전건 원문 adjudicate 완료 (UNIT_MODIFIER_UG_RAE 3 · FN_MODE_COLON 2)
비타민E: spec+명시귀속 23 전건 포함 · spec만(부원료) 0 · 모호 0
Agent B 대상: RETIRE_REPLACE 31 (즉시 26 · 파서수정 선행 5)
파일럿 재개: 가능(파서 2건 수정 선행 권고, 미수정 시 26건 한정)
DB write 0 · canonical 변경 0 · apply 미실행
```

---

*read-only · DB write 0 · canonical 변경 0. 고N 3건 등 31 외 대상 미포함.*

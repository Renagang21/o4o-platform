# CHECK — 100건 제한 대량작업 · 체크포인트 4 (20건)

- **WO**: `WO-O4O-HFF-DESCRIPTION-PROBIOTICS-LIMITED-BULK-100-V1`
- **일자**: 2026-07-16 · **판정**: **PASS** (BLOCKED 0 / PAUSE 사유 없음)
- **DB write**: 0 · **migration**: 0

---

## 1. 결과 집계

```text
선정                    20
작성 대상               20
HOLD                     0
작성 완료               20
```

| 분모 | PASS | REVIEW | BLOCKED | HOLD |
|---|:--:|:--:|:--:|:--:|
| **작성 대상 20건** | **20** | 0 | **0** | — |
| **전체 선정 20건** | **20** | 0 | 0 | **0** |

## 2. 파서 상태

작성 대상 20 × 3필드 = 60

| 상태 | 건수 |
|---|:--:|
| `PARSED` | **60** |
| `ABSENT` / `PARSE_FAILED` / `ABNORMAL` | 0 / 0 / 0 |

| 항목 | 결과 |
|---|:--:|
| 원문 파싱값 vs 수작업 grounding 불일치 | **0 / 20** |
| 파서가 새로 차단한 제품(벌크) | 0 |

## 3. CP4 선행 — 원문 필드 절단 전수 검색 (지시)

`slice / substring / substr / truncate / .{0,N} / 길이 하드코딩` 을 전수 검색했다.

| 결과 | 내용 |
|---|---|
| **가드 규칙의 `truncate(input.source.*)`** | **전부 sourceEvidence·message 표시용**. 판정 입력은 자르지 않는다(정규식은 원문 전체에 적용). → 가드 계층은 이미 전문을 판정에 쓴다 |
| **발견된 결손 1건** | `Q-TRUNCATED` 의 `findTruncation` 이 셀 앞 레이블("보관 ") 때문에 `indexOf(cell)=-1` 로 절단을 놓쳤다(CP2 서흥). → **셀 꼬리 24자 앵커**로 수정 |
| **파편 판정 정밀화** | "마지막 1음절"은 거칠어 `…서늘한 곳`(원문 `곳에`)을 파편 오판. **원문 다음 글자가 조사·어미면 완전한 단어**(요약 REVIEW), 아니면 파편(BLOCKED) |

### 전수 감사 결과 (재빌드 없이 93건)

| 배치 | 파편(BLOCKED) | 요약(REVIEW) |
|---|:--:|:--:|
| 30-A / 30-B | 0 | 3 / 1 (초기 배치 보관 축약) |
| 그 외 재빌드 배치 | 0 | 0 |
| **CP4** | **0** | **0** |

→ `hff-truncation-audit.ts` 스크립트로 상시 점검 가능.

## 4. 실화면 검수 — **자동검사가 못 잡은 2건** (대표 5건)

레이아웃·overflow 는 정상이었으나 눈으로 보니 자동 파생 결함 2건.

| # | 결함 | 원인 | 조치 |
|---|---|---|---|
| 1 | 표시 기준 행 끝 `…이상 3)` | 절 인용에 **다음 항목 번호** `3)` 이 딸려옴 | 절단 경계에 항목 번호(`N) `·`N. `) 추가 |
| 2 | 섭취방법에 `입안에서 녹여 섭취` 누락 | `프로바이오틱스 키즈 딸기맛`·`뉴메이트` 는 **입안에서 녹여** 섭취(신규 특수 원문)인데 칩에 없었다 | 녹여·씹어 칩 반영 |

> ①의 항목 번호 정규식이 처음엔 `(1억)` 의 괄호 안 숫자를 경계로 오인해 표시 기준을
> `…(100,000,000(1억) cfu` 에서 잘랐다(이지푸·빅히트 BLOCKED). **`Q-TRUNCATED 가 이 절단을 잡았다**
> — 자기 규칙이 자기 작성기 버그를 검출한 셈이다. `N) ` 처럼 **뒤에 공백**을 요구해 괄호 내부를 배제.

## 5. CP4 신규 지표 (WO 지시)

| 지표 | 값 |
|---|:--:|
| 원문 필드 절단 발견 수 | **0** (§3 감사) |
| 절단 때문에 근거 매칭이 달라진 건수 | **0** |
| 제품명·원문 인용 오탐 수 | **0** (§4 ② 항목번호 오탐은 절단이지 인용 오탐 아님) |
| 실제 비교·우월 표현 차단 수 | **0** (원문에 비교 주장 없음) |
| HOLD_GROUNDING 수 | **0** |

독립 검증(가드와 별개):
- 제품명 밖 연령 소구 **0** (`키즈 딸기맛`·`데일리 수퍼 키즈` 제품명 표기만)
- 실제 비교·우월 표현 **0**

## 6. 반응형

| 지표 | 값 |
|---|:--:|
| 자동검사 대상 수 | **200** (20 × ko/en × 5뷰포트) |
| overflow / 잘림 / 겹침 / ko-en 오류 | 0 / 0 / 0 / 0 |
| 5뷰포트 각 PASS | 40/40 · 40/40 · 40/40 · 40/40 · 40/40 |
| 수정 후 잔여 오류 | 0 |

## 7. 특이 원문 (보고)

| 제품 | 원문 | 처리 |
|---|---|---|
| 코어리셋 옴니바이오틱스 | 용법 2가지 `1일 2회 1캡슐(0.5g)` / `1일 1회 2캡슐(1g)` | 기준량 1g = 1일 총량 |
| 모거티움 | `2캡슐(900mg)` 합계 | `servingTotalWeight`, per-unit 금지 |
| 프로바이오틱스 키즈 딸기맛 · 뉴메이트 | `입안에서 녹여 섭취` | **신규 특수 원문** — 칩에 반영 |

## 8. 중지 기준

| 기준 | 해당 |
|---|:--:|
| 파서와 원문 수치 불일치 | 아니오 (0) |
| ABNORMAL 인데 초안 수치 생성 | 아니오 (0) |
| PARSE_FAILED 를 ABSENT 로 처리 | 아니오 |
| 동일한 신규 실패 유형 2건 이상 | **아니오** — 신규 실패 유형 0 |
| ko/en 단위 환산 불일치 | 아니오 (0) |
| 공통 CSS·렌더러 문제 | 아니오 |

→ **CP5 진행 가능.**

## 9. 검증

| 항목 | 결과 |
|---|---|
| 테스트 | **120 PASS** |
| 과거 오류 회귀 | 10/10 유지 |
| 전 배치 회귀 | **BLOCKED 0** (25 P13/R12 · 30-A P7/R3 · 30-B P9/R1 · 30-C 10 · CP1 18 · CP2 20 · CP3 19 · CP4 20) |
| tsc (content-guard) | 0 |

## 10. 진행 현황

```text
CP1 ✅ 18 (HOLD 2: SOURCE_ABNORMAL, UNSUPPORTED_DIMENSION)
CP2 ✅ 20 (HOLD 0) + 반응형 최초 적용
CP3 ✅ 19 (HOLD 1: GROUNDING) + Q 품질 규칙
CP4 ✅ 20 (HOLD 0) + 절단 전수 감사
CP5 ▶ 다음 (20건)
누적 작성 77 / HOLD 3 / BLOCKED 0
```

# CHECK — 유산균 생산 배치 (신규 200) · A-CP02 (20건)

- **WO**: `WO-O4O-HFF-DESCRIPTION-PROBIOTICS-PRODUCTION-BATCH-001-V1`
- **일자**: 2026-07-17 · **판정**: **PASS** (최종 BLOCKED 0)
- **DB write**: 0 · **migration**: 0

---

## 1. 결과 집계

```text
선정 20 / 작성 20 / HOLD 0 / 작성 완료 20
초안 1차 → BLOCKED 3 (신규 실패 2유형) → 근본 원인 수정 → 최종 PASS 20 / REVIEW 0 / BLOCKED 0
```

## 2. 파서 상태 (독립 대조)

- 파서 사전 스캔 60 PARSED · ABSENT/PARSE_FAILED/ABNORMAL 0 · 벌크 0
- 원문 파싱값 vs 수작업 grounding 불일치 **0 / 60** · 절단 감사 파편 0 / 요약 0

## 3. 신규 실패 유형 2건 — 근본 원인 수정

초안 1차 guard 에서 BLOCKED 3건. **두 가지 신규 실패 유형**이라 원인을 확정하고 최소 수정.

### 3-1. D-CLAIM-UNGROUNDED-001 "안심하고" (엔티에이치·V21, 대웅 2건) — **작성기 수정**
- 원인: 두 제품의 **보관 원문**에 판촉 문장 `"…제품 품질에는 이상이 없으니 안심하고 섭취하십시오"` 가 섞여 있고, 내가 보관 원문을 통째 인용해 그 문장이 카드에 들어감. `srcHas` 에 '안심' grounding 경로가 없어(설계상 안심=항상 근거 없는 주장) 가드가 **정확히** 차단.
- 수정(작성기 `acp-write.mjs`): 보관 인용 시 **주장 문장만** 문장 단위로 제외(`cleanStorage`). 보관 지시 문장은 자르지 않음. 주장 문장이 없으면 원문 무변경(기존 동작 유지).
- 결과: 보관 행 = `직사광선 및 고온, 다습한 곳을 피하여 서늘한 곳에 보관하십시오. 영, 유아의 손이 닿지 않도록 주의하시기 바랍니다.` (지시+아동안전 보존, 판촉 제거) — 실화면 확인.

### 3-2. B-SPEC-MINMAX-COMPARE-004 "at least" (땡큐) — **가드 오탐 수정(정지조건 7)**
- 원인: 규격어(at least) 비교문맥 판정 정규식 `/(than|…)/i` 가 제품명 **"**Than**k You"** 의 `Than` 을 비교어 `than` 으로 오탐(이 블록은 name-strip 없이 raw HTML 스캔). → 정상 규격 인용이 비교 주장으로 BLOCKED.
- 수정(가드 `product-description-guard.rules.ts`, **단어 경계**): `than` → `\bthan\b`. 최소 수정, 커밋 전후 Read 로 Agent B 충돌 확인.
- 회귀 방지 쌍 테스트 2건 추가: (a) "Thank You…at least…" 는 COMPARE-004 오탐 없이 INFO, (b) 실제 "more … than other brands" 는 COMPARE-004 BLOCKED 유지(검출력 유지).

## 4. 실화면 검수에서 추가로 잡은 것 — **괄호 항목번호 누출 (자동검사 전부 통과)**

`(1)(2)(3)` 번호 원문에서 표시기준 인용 절 끝에 **다음 시험항목의 괄호 번호 `(3)` 가 딸려옴**
(`…이상 (3)`). `)` 가 keep 문자라 기존 trailing 정리에 안 걸리고, 파서/절단감사/가드/영문 전부 **미검출** — 실화면 검수만 검출.
- 수정(작성기): 절 끝의 **순수 괄호숫자** `(N)` 만 제거(`(2 g)`·`(100억)` 은 보존).
- **CP01 소급 정정 3건**: `파파 파르마 키즈`·`Royale`·`아이배냇` (ko 인용에 `(3)` 누출) — CP02 에서 발견해 소급 수정. CP01 재-guard PASS 20 유지.

## 5. 반응형 · 품질

- 200 조합(20 × ko/en × 5뷰포트) 전부 PASS · overflow/잘림/겹침/ko-en 오류 0
- 제품명 밖 연령/가족 소구 0 · 비교·우월 표현 0 · HOLD 0

## 6. 사람검수 대상 (CP5 교훈 트리거)

HR ≈ 7건(calc=true 6 + 1일 2회 1) + 1회 2캡슐 1건 별도. 대표 실화면 검수:

| 제품 | 초점 | 결과 |
|---|---|---|
| 2:1 ratio fiber + Kimchi (1회 2캡슐) | 캡슐당 파생 여부 | `1회 2캡슐` 표기, 900mg 기준량 그대로, **캡슐당 균수 파생 없음** (정상) |
| 위담 알파 (1일 2회) | CP5 사실오류 | `1일 2회 1포`, "1회=하루" 없음, 포당 파생 없음 (정상) |
| 발리 프리미엄 (per-unit capsule) | 1캡슐(450mg) 근거 | 1캡슐=450mg=기준량 명시 → 캡슐당 10억 CFU **기준량과 동일**(파생 아님) |
| 팜스슈퍼 키즈 / 엔티에이치 패밀리 | 이름만 키즈·가족 | 제품명만, 연령·가족 소구 0 |
| 닥터프로바 (trailing 표시량 `CFU/3g (표시량 이상)`) | 인용 파편 | fallback 앵커로 완전 인용, 파편 없음 |

## 7. 중지 기준

| 기준 | 해당 |
|---|:--:|
| 새 실패 유형 → 수정 후 BLOCKED 잔존 | **아니오** (수정 후 0) |
| 동일 오류 반복 | 괄호번호 누출 CP01 3 + CP02 3 — **작성기 1회 수정으로 전량 해소** |
| 파서 vs 수작업 불일치 | 아니오 (0) |
| HOLD 미분류 | 아니오 (HOLD 0) |
| 서술 논리 오류 반복 | 아니오 |

→ 신규 실패 2유형 모두 근본 원인 수정·회귀 확인 완료. **A-CP02 종료, A-CP03 진행.**

## 8. 검증

| 항목 | 결과 |
|---|---|
| content-guard 테스트 | **122 PASS** (120 + 오탐/검출력 쌍 2) |
| 전 배치 회귀 (guard all) | BLOCKED 0 — 25·30A~C·CP1~5·A-CP01·A-CP02 전부 |
| 과거 오류 회귀(known-errors) | 유지 (jest 포함) |
| 가드 수정 | 1 (오탐 단어경계, 완화 아님 — 검출력 쌍 테스트 고정) |
| 작성기 수정 | 2 (보관 판촉문장 제외 / 괄호 항목번호 제거) |

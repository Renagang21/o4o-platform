# CHECK-O4O-HFF-KO-MISSING-FUNCTION-CONTAINER-AND-PROBIOTICS-WOMAN-RECOVERY-V1

`#35 프로바이오틱스우먼` 기능성 컨테이너 부재 — **조사·제안 단계** (DB write 0)

- 근거 WO: `WO-O4O-HFF-KO-MISSING-FUNCTION-CONTAINER-AND-PROBIOTICS-WOMAN-RECOVERY-V1`
- 선행: `CHECK-O4O-HFF-KO-FUNCTION-HUMAN-REVIEW-PILOT-FRAGMENTED-AND-MALFORMED-47-V1` (`#35` = `BLOCKED_RENDERER_STRUCTURE`)
- 착수 HEAD: `01a3cf5db`
- 판정: **조사 완료 · 제안 2안 검증 통과 · 미적용**

---

## 0. 실행 조건

| 항목 | 결과 |
|---|---|
| `ide_selection` 자동 첨부 | **없음** |
| **요청서 수신 상태** | **§1 목표 중간에서 절단** — §2 이후(작업 성격·Apply 게이트·산출물 파일명·중지 조건·실행 승인) 부재 |
| 사용자 결정 | **"조사·제안만, write 없음"** + 요청서 재전송 예정 |
| DB write | **0** (전 세션 `SET default_transaction_read_only = on`) |
| 공용 driver·parser·renderer·CSS·원본 큐 | **미수정** |
| 작업트리 | `MM pnpm-lock.yaml` (타 세션) — **미접촉** |

---

## 1. 요청서 전제 정정 2건

### 정정 A — "기능성 3절 전량 미노출" 은 컨테이너 기준

`#35` canonical 의 `sd-intro` 문단에 다음이 이미 존재한다.

> "…**유산균 증식 및 유해균 억제, 배변활동 원활, 장 건강에 도움을 줄 수 있는** 프로바이오틱스를 담았습니다."

| 구분 | 사실 |
|---|---|
| 전용 기능성 섹션(컨테이너) | **없음** ✅ 요청서 지적 정확 |
| 기능성이 문서에 전혀 없음 | **아님** — 산문에 언급 존재 |
| 절 단위 식별 가능 여부 | **불가** — 3절이 쉼표로 병합, 공식 `장건강` → 본문 `장 건강` 로 변형 |
| 실측 (렌더 baseline) | 목록 항목으로 렌더되는 공식 절 **0 / 3** |

즉 "미노출" 은 **절 단위 식별 가능한 형태로 미노출**이 정확한 표현이다. 파일럿 보고서의 표현이 요청서에 그대로 옮겨진 것으로 보인다.

### 정정 B — 본 CHECK 작성 중 발생한 **본인 오류** 정정

조사 초기에 다음과 같이 보고했으나 **틀렸다.**

> ~~"왜-family 는 설계상 전용 기능성 섹션이 없다. `#35` 는 15,402건 family 의 대표 사례다."~~

| 항목 | 내용 |
|---|---|
| 원인 | 기능성 섹션 보유 판정을 **driver 어휘 `주요 기능성` 단독 `LIKE`** 로 수행 |
| 실제 | 왜-family 는 **자체 어휘**를 사용한다 — `원료별 공식 인정 기능성` / `공식 인정 기능성` / `<원료> 영양기능 (공식 인정 기능성)` / `<원료> 기능성 (공식 인정)` |
| 교정 판정축 | `content ~ '<h2>[^<]*기능성[^<]*</h2>'` (헤딩 어휘 무관) |
| 교정 결과 | 기능성 섹션 부재는 **15,498건 → 826건** |

`#35` 는 15,402건의 대표가 아니라 **826건 소수 집합**에 속한다.

---

## 2. 전수 감사 (교정된 판정축)

| 지표 | 값 |
|---|---|
| HFF STORE/ko canonical 총계 | **40,913** |
| 기능성 섹션 보유(어휘 무관) | **40,087** (98.0%) |
| **기능성 섹션 부재** | **826** (2.0%) |
| 그중 왜-family | **826** (전량) |
| driver 어휘 `<h2>주요 기능성</h2>` | 25,415 |
| 왜-family 문서 | 15,371 |

### 2-1. 기능성 섹션이 실제로 없는 826건의 헤딩 시그니처

| 시그니처 | 건수 |
|---|---|
| `왜 이 제품인가 \| 섭취방법 \| 표시 기준 \| 이런 분께` | **761** |
| `왜 이 제품인가 \| 섭취방법 \| 표시 기준 \| 보호자 안내` | 59 |
| `왜 이 제품인가 \| 섭취방법 \| 표시 기준 (액상) \| 보호자 안내` | 4 |
| `왜 이 제품인가 \| 섭취방법 \| 표시 기준 (액상) \| 이런 분께` | 2 |

`#35` 는 최상위 시그니처(761건)에 속한다. **동일 시그니처 배치 확장이 가능한 구조**다.

### 2-2. 왜-family 가 실제 사용하는 기능성 헤딩 어휘

| 헤딩 | 건수 |
|---|---|
| `원료별 공식 인정 기능성` | 8,277 |
| `공식 인정 기능성` | **2,226** |
| `비타민 D 영양기능 (공식 인정 기능성)` | 448 |
| `MSM 기능성 (공식 인정)` | 384 |
| `비타민 C 영양기능 (공식 인정 기능성)` | 332 |
| `아연 영양기능 (공식 인정 기능성)` | 289 |
| `이 홍삼의 공식 기능성` | 271 |
| `식이섬유 기능성 (공식 인정)` | 204 |
| `루테인 기능성 (공식 인정)` | 174 |
| `밀크씨슬 기능성 (공식 인정)` | 170 |

→ 복구 어휘는 **문서가 속한 family 의 자체 헤딩**을 따라야 한다. driver 의 `주요 기능성` 을 쓰면 family 계약과 불일치한다.

### 2-3. 부재 그룹 표본 분류 (결정적 표본 400건 · 전수 아님)

| 분류 | 건수 |
|---|---|
| 공식 원문에서 절 경계 확정 가능 | **218** |
| 그중 절 전량이 이미 산문에 존재 | 195 |
| 그중 절 일부가 문서에 없음 | 23 |
| 절 경계 미확정(마커 부재) | **182** |
| 공식 원문 없음 | 0 |

---

## 3. `#35` 공식 원문과 복구 대상

```
[프로바이오틱스 제품①유산균 증식 및 유해균 억제에 도움을 줄 수 있음 ②배변활동 원활에 도움을 줄 수 있음 ③장건강에 도움을 줄 수 있음
```

| 항목 | 값 |
|---|---|
| 라벨(첫 마커로 유일 확정) | `프로바이오틱스 제품` |
| 공식 기능성 절 | 3절 (유산균 증식·유해균 억제 / 배변활동 원활 / 장건강) |
| 원문 verbatim 여부 | **전량 true** |
| 대괄호 상태 | 닫는 대괄호 누락 (`[` 1 / `]` 0) |

파일럿에서 `FUNCTION_BLOCK_ABSENT_NO_TEMPLATE` 로 차단한 이유는 **동일 문서 내 복제 가능한 라벨+목록 마크업이 없었기** 때문이다. 재조사 결과 이 문서는 `왜 이 제품인가` 에서 **`<ul class="sd-why">` 를 이미 사용**하므로, 마크업 자체는 문서 내 재사용이 가능하다. 남은 결정 사항은 **헤딩 어휘**뿐이다.

---

## 4. 제안 2안 (미적용)

| | 안 A (**권장**) | 안 B |
|---|---|---|
| 헤딩 | `공식 인정 기능성` (family 자체 어휘 · 선례 2,226건) | `주요 기능성` (driver 표준) |
| 위치 | `왜 이 제품인가` 직후 / `섭취방법` 앞 | `sd-intro` 직후 (최상단) |
| 마크업 | 문서 내 기존 `<ul class="sd-why">` 재사용 | 동일 |
| 길이 증가 | +133 | +130 |
| 구조 검증 | **전 항목 PASS** | **전 항목 PASS** |
| 렌더 (430/820/1280) | **PASS** · 절 3/3 | **PASS** · 절 3/3 |
| 트레이드오프 | family 계약과 완전 정렬 · 배치 확장 가능 | 플랫폼 표준 어휘와 정렬하나 family 어휘와 불일치 |

두 안 공통 검증 결과: 미정의 class 0 · 태그 균형 · 기존 헤딩 전량 보존 · `sd-foot` 보존 · `sd-intro` 보존 · 빈 항목 0 · 삽입 절 전량 원문 verbatim.

### 4-1. 안 A 삽입 블록

```html
<h2>공식 인정 기능성</h2><ul class="sd-why"><li>유산균 증식 및 유해균 억제에 도움을 줄 수 있음</li><li>배변활동 원활에 도움을 줄 수 있음</li><li>장건강에 도움을 줄 수 있음</li></ul>
```

### 4-2. 안 A 의 하위 선택지 (후속 WO 결정 필요)

단일 원료 제품이므로 다음 두 어휘가 모두 family 선례를 갖는다.

| 후보 | family 선례 |
|---|---|
| `공식 인정 기능성` (본 제안 채택) | 2,226건 |
| `프로바이오틱스 기능성 (공식 인정)` | `MSM 기능성 (공식 인정)` 384 · `루테인 기능성 (공식 인정)` 174 등 원료명 지정 패턴 |

## 5. 렌더 검증 — 래퍼 computed-style 증명

| 속성 | 래퍼 **없이** | 래퍼 **적용** |
|---|---|---|
| `.sd-card` max-width | `none` | **`860px`** |
| `.sd-card` border-radius | `0px` | **`20px`** |
| `.sd-hero` padding | `0px` | **`40px 34px 32px`** |
| `.sd-badge` border-radius | `0px` | **`999px`** |

`cssActuallyApplied: true`. 현재 LIVE baseline 은 공식 절이 목록 항목으로 **0/3**, 두 제안은 **3/3**.

## 6. 범위 외 정책 불일치 (보고만)

| ID | 내용 | 건수 |
|---|---|---|
| `PM1-IRON-BUNKKE` | `이런 분께` 섹션 — 최근 WO 들이 driver 산출물에 대해 **금지 항목**으로 명시(구매지원 내러티브 축) | **15,435** |
| `PM2-NO-EXPERT-FOOTER` | `매장 내 약사 등 전문가` 문의 footer 부재 | **13,955** |

두 항목은 왜-family 저작 계약에서 기인하며 이번 범위 밖이다. 별도 정책 결정이 필요하다.

## 7. 산출물

```
apps/api-server/src/scripts/data/hff-ko-missing-function-container-35-proposal-v1.json
apps/api-server/src/scripts/data/hff-ko-missing-function-container-35-render-audit-v1.json
apps/api-server/src/scripts/data/hff-ko-missing-function-container-family-audit-v1.json
apps/api-server/src/scripts/data/hff-ko-missing-function-container-absent-classification-v1.json
apps/api-server/src/scripts/hff-ko-missing-function-container-audit.mjs
apps/api-server/src/scripts/hff-ko-missing-function-container-render.mjs
docs/checks/CHECK-O4O-HFF-KO-MISSING-FUNCTION-CONTAINER-AND-PROBIOTICS-WOMAN-RECOVERY-V1.md
```

`applied: false` · `dbWrites: 0` 을 산출물에 명시했다.

## 8. 후속 권고

1. **재전송될 요청서(§2 이후)** 에 다음을 명시할 것 — 복구 어휘(안 A / 안 B / 원료명 지정), 대상 범위(`#35` 단건 vs 761 시그니처 vs 826 전량), Apply 게이트, 중지 조건.
2. 826건 중 **절 경계 확정 가능 표본 비율 218/400** 을 고려하면 전량 자동 복구는 불가하다. 마커 부재 182/400 은 사람 경계 확정이 필요하다.
3. `PM1` / `PM2` 는 15,435 / 13,955 규모의 별도 정책 WO 로 분리할 것.

## 9. 함정 기록

1. **family 어휘로 존재 판정하지 말 것** — `주요 기능성` 단독 `LIKE` 는 왜-family 15,371건을 전부 "기능성 없음" 으로 오판한다(실제 826). 헤딩 정규식(`<h2>[^<]*기능성[^<]*</h2>`)으로 판정할 것.
2. `공식 인정 기능성` 문자열 `LIKE` 는 driver hero 의 `공식 인정 기능성 기반 매장 설명서` 문구에도 걸린다(38,097건) — **헤딩 매칭과 본문 매칭을 구분**할 것.
3. `is-solid` 클래스는 renderer 에 정의되어 있다. 파일럿에서 쓴 DEFINED 집합에는 없었으므로 왜-family 문서 검증 시 반드시 추가할 것.
4. 왜-family 문서는 `sd-who`(이런 분께)·`small`·`b` 태그를 사용한다 — 태그 균형 검사 대상에 포함할 것.

---

*작성: 2026-07-30*

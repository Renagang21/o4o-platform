# CHECK — 유산균 생산 배치 (신규 200) · A-CP03 (20건)

- **WO**: `WO-O4O-HFF-DESCRIPTION-PROBIOTICS-PRODUCTION-BATCH-001-V1`
- **일자**: 2026-07-17 · **판정**: **PASS** (작성 19 PASS 19 / BLOCKED 0) · **신규 HOLD 유형 1건 발생 → §3, 보고 후 CP04 진행 대기**
- **DB write**: 0 · **migration**: 0

---

## 1. 결과 집계

```text
선정 20 / 작성 19 / HOLD 1 / 작성 완료 19
작성 대상 19 → PASS 19 / REVIEW 0 / BLOCKED 0 (초안 1차 BLOCKED 1 → 가드 오탐 수정 후 0)
```

## 2. 파서 상태

- 작성 19 × 3필드 = 57 PARSED · ABSENT/PARSE_FAILED/ABNORMAL 0 · 벌크 0
- 원문 파싱값 vs 수작업 grounding 불일치 **0 / 57** · 절단 감사 파편 0 / 요약 0

## 3. ★ 신규 HOLD 유형 — `HOLD_MULTI_FUNCTIONAL` (기존 3종으로 분류 안 됨 → 정지조건 5)

`쾌변엔 식이섬유 유산균` (오투바이오, permit 20130020008439) 의 표시기준에
**프로바이오틱스 외 두 번째 기능성 지표 `식이섬유(%) : 표시량[5g/12g]의 80% 이상`** 가 별도 존재.

- 순수 프로바이오틱스 템플릿(mainFunction=프로바이오틱스 3기능)이 **식이섬유 기능을 누락**하고, `표시량` 앵커가 식이섬유 절을 먼저 집는다.
- 기존 HOLD 3종(SOURCE_ABNORMAL / UNSUPPORTED_DIMENSION / GROUNDING) 어디에도 안 맞음 → **신규 코드 `HOLD_MULTI_FUNCTIONAL` 로 격리**(초안·입력 JSON 미생성, hold JSON 에만 기록).
- **전체 200 중 동일 유형 3건**: A-CP03 `쾌변엔 식이섬유 유산균` · A-CP07 `생유산균화이버` · A-CP08 `유지연의 쾌변엔 장 건강` (전부 식이섬유+프로바이오틱스 복합).

> **보고 필요(정지조건 5)**: 이 3건을 (a) 순수 프로바이오틱스 배치에서 **제외**할지, (b) 식이섬유 기능을 포함한 **복합 템플릿 변형**으로 별도 처리할지 판단 요청. 판단 전 격리 유지.

## 4. 초안 1차 BLOCKED 1건 — 가드 오탐 수정 (정지조건 7)

`에이엠비` 원문 `표시량｛100,000,000 CFU/2,000mg(1포) 이상｝` (**전각 중괄호 ｛｝**).
- Q-TRUNCATED-002 BLOCKED. 원인: `findTruncation` 어절경계 문자군 `/[\s.,)\]}·ㆍ]/` 에 **전각 닫음괄호 미포함**. 셀에서 전각 `｝` 는 TRAILING_JUNK 로 제거되고 원문엔 남아 `next=｝` 가 되는데, 이를 경계로 못 봐 **파편으로 오판**.
- 수정(가드): 경계 문자군에 전각 닫음괄호 `）］｝」』` 추가. 완화 아님(전각 닫음괄호는 실제 어절 경계).
- 회귀 테스트 1건 추가(A-CP03 실측 재현): 전각 `｝` 로 끝나는 규격 인용은 절단 오탐 없음.
- 작성기(scratchpad): 인용 trailing 정리 keep-class 에도 전각 닫음괄호 추가(균형 괄호 보존).

## 5. 반응형 · 품질

- 190 조합(19 × ko/en × 5뷰포트) 전부 PASS · overflow/잘림/겹침/ko-en 오류 0
- 제품명 밖 연령/가족 소구 0 · 비교·우월 표현 0

## 6. 사람검수 대상 (CP5 교훈 트리거) — 대표 실화면 검수

HR 5건(calc=true 4 + 1일 2회 1). 대표 검수 결과 결함 0:

| 제품 | 초점 | 결과 |
|---|---|---|
| 그린퍼맨-19S (1일 2회) | CP5 사실오류 | `1일 2회 1포`, "1회=하루" 없음, 포당 파생 없음 (정상) |
| 슈퍼 100억 (녹여 + per-unit) | 포당 균수 근거 | 기준량 2g=1포=1회 명시 → 포당 100억 CFU **기준량과 동일** |
| 시그니처 R (**원문에 "이상" 없음**) | 규격어 임의 추가 | 배지·본문·인용 모두 "이상" **미표기**(원문 그대로) |
| 에이엠비 (전각 ｛｝) | 인용 균형 | `표시량｛…(1포) 이상｝` 균형 인용, 파편 없음 |
| 다나운 EMR-PP (장용성·긴 이름) | 레이아웃 | 전 뷰포트 정상 |

## 7. 알려진 사항(보고, 차단 아님)

- 인테이크 칩: 원문이 `그대로 섭취`(direct)만 있을 때도 칩이 `직접 또는 물과 함께`로 `물`을 부가함. **155 베이스라인·CP01/02 와 동일한 기존 작성기 동작**(가드 무플래그). 프로그램 전반 정합 차원에서 완화 여부는 코디네이터 판단 대상으로 보고. 이번 배치에선 기존 동작 유지.

## 8. 검증

| 항목 | 결과 |
|---|---|
| content-guard 테스트 | **123 PASS** (전각 경계 오탐 테스트 +1) |
| 전 배치 회귀 (guard all) | BLOCKED 0 — 25·30A~C·CP1~5·A-CP01~03 전부 |
| 가드 수정 | 1 (전각 닫음괄호 경계, 완화 아님) |
| 작성기 수정 | 1 (인용 keep-class 전각 닫음괄호) + HOLD 라우팅 |

→ **작성 19 PASS. HOLD_MULTI_FUNCTIONAL 신규 유형 발생으로 정지조건 5 — 보고 후 CP04 진행.**

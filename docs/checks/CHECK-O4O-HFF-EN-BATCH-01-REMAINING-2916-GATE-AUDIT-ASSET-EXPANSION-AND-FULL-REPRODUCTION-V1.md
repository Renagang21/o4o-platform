# CHECK-O4O-HFF-EN-BATCH-01-REMAINING-2916-GATE-AUDIT-ASSET-EXPANSION-AND-FULL-REPRODUCTION-V1

잔여 2,916건 게이트 감사 — **286건 추가 생산**, 누적 Batch 01 완료 **2,370**

- 기준 커밋: `ef0cf9344` · 착수 HEAD `ef0cf9344` (= `origin/main`)
- 판정: **PASS** — INSERT 286 · EN canonical 16,780 → **17,066** · KO 불변

---

## 1. 차단 원인 전수 분해 (지시된 1순위)

재실행 전 분포를 다시 산출했다.

| blocker | 문서 수 | 성격 |
|---|---:|---|
| `NO_ENTRY` (번역 자산 부족) | **2,614** | 정당 |
| **`FALSE_GATE`** | **286** | **게이트 결함** |
| `NUMBER_ONLY` | **16** | **정당한 차단** (실제 용량 누락) |
| `STRUCTURE` | 0 | — |

| 미커버 문구 수 | 문서 수 |
|---|---:|
| 0개 | 302 |
| 1개 | **853** |
| 2개 | 609 |
| 3개 | 425 |
| 4개 이상 | 727 |

남은 고유 문구 **3,884종**, 마지막 문구 집합 **690종**(전량 확정 시 853건 해소 기대).

---

## 2. FALSE_GATE 286건 — 중복 수치 검사

슬롯별(번역 쌍 단위) 수치 검증을 이미 수행하는데, **문서 전체 수치 비교**를 한 번 더 하고 있었다. 문서 전체 검사는 번역 대상이 아닌 영역(`sd-spec` 등, 원문 그대로 유지)까지 끌어들여 오탐만 만든다.

슬롯별 검사가 더 정확하므로 **중복 검사를 제거**했고, 정확히 286건이 해소됐다(1,282 → 1,568).

---

## 3. NUMBER_ONLY 16건 — 게이트가 옳게 막았다

```
KO  1일 1회, 1회 1포, 1포 80ml          EN  Take 1 stick pack once a day.        ← 80ml 누락
KO  1일 3회, 1회 2스푼(1g/1스푼) …       EN  Take 2 spoonfuls 3 times a day …     ← 1g 누락
KO  1일 1회, 1회 1포(1포 : … 500mg X 1C, … 1022mg X 1C) …                        ← 성분량 누락
```

부가 용량 표기(`1포 80ml`, `1g/1스푼`, 복합 구성)를 파서가 흡수하지 못한 경우다. **실제 복용량 정보 손실**이므로 HOLD 유지가 옳다.

---

## 4. 재생산 결과 (합계 4,209 ✅)

| 상태 | Track A(33) | Track B(4,176) | 계 |
|---|---:|---:|---:|
| CREATED_NEW_EN | — | **1,568** | **1,568** |
| RESOLVED_NO_CHANGE | 11 | — | 11 |
| HOLD_TRANSLATION | 22 | 2,608 | **2,630** |
| FAILED_SYSTEM | 0 | 0 | **0** |

Track A 11건은 직전 라운드에서 이미 적용되어 `RESOLVED_NO_CHANGE` 로 정상 인식됐다.

---

## 5. 렌더 · Apply

**렌더 PASS** — 시그니처 전수 커버 + 고위험 전량, 292문서 × 3폭 = **876 렌더**. 한국어 노출 · 라벨 손실 · 개별인정번호 손실 · overflow 등 **전 항목 0**. 래퍼 증명 `"" → 860px`.

**Apply** — shard 500 × 4.

| 항목 | 값 |
|---|---:|
| expected INSERT | 1,568 |
| **actual INSERT** | **286** |
| skipped (이미 적용분) | **1,282** |
| shard rollback | 0 |
| **KO canonical** | 40,918 (불변) |
| **EN canonical** | 16,780 → **17,066** |

skip 1,282건은 직전 라운드에서 생성된 것으로, **저장 직전 중복 재확인 가드가 정상 작동**한 결과다.

---

## 6. 독립검증 (별도 read-only 세션) — **PASS**

new hash **1,568 / 1,568** · 기능성 섹션 내 한국어 **0** · 필드 drift **0** · **KO drift 4,209 전수 0** · EN canonicalDup **0** · EN 증가량 예상 286 = 실제 **286** · 상태 합계 **4,209** · HOLD 큐 중복 **0**.

DB 실측: EN 미보유 KO **24,138 → 23,852** · 기능성 없는 EN **22**.

---

## 7. 후속 배치 공통 계약 (고정)

이번까지 확인된 게이트 계약을 고정한다.

1. 한국어 잔존 검사는 **번역 대상 슬롯**에만 적용한다(제품명 등 고유명사 제외)
2. 섭취방법 번역은 **괄호 안 용량·단위까지 보존**한다
3. `<li><b>라벨</b>` 을 번역 coverage 와 렌더 손실 검사에 포함한다
4. **수치 검증은 슬롯별(번역 쌍 단위)로만** 한다 — 문서 전체 비교는 중복이며 오탐을 만든다
5. `expected > 0, actual = 0` 이면 DB 실패로 단정하지 말고 **필드명·manifest 계약**부터 점검한다
6. 재실행 전 **`NO_ENTRY / FALSE_GATE / NUMBER_ONLY / STRUCTURE` 분포를 항상 재산출**한다

---

## 8. 누적 현황

| 항목 | 값 |
|---|---:|
| **Batch 01 누적 완료** | **791 + 1,293 + 286 = 2,370** |
| 남은 HOLD | **2,630** |
| EN canonical | **17,066** |
| EN 미보유 KO | **23,852** |
| 기능성 없는 기존 EN | **22** |

---

## 9. 다음 라운드

게이트 결함은 이번으로 소진됐다(`FALSE_GATE 0` 예상). 남은 **2,630건은 전부 실제 번역 자산 부족**이며, 병목이 명확히 자산으로 이동했다.

다음 목표: **마지막 문구 집합 690종 전량 확정** → 853건 즉시 해소. 이후 미커버 2개 집합(609건)으로 이동.

---

## 10. 함정 기록

1. **중복 게이트는 오탐만 만든다.** 슬롯별 검증이 있으면 문서 전체 검증은 빼라. 번역 대상 밖 영역을 끌어들인다.
2. **NUMBER_ONLY 는 대부분 정당하다.** 이번 16건은 전부 실제 용량 누락이었다. 완화하지 말고 파서를 고치거나 HOLD 하라.
3. **재적용 시 skip 은 정상이다.** 이미 생성된 canonical 을 중복 INSERT 하지 않는 가드가 작동한 것이다.

---

*작성: 2026-07-31*

# CHECK-O4O-KPA-TABLET-CORNER-CURRENT-PREVIEW-COMPACT-V1

> WO: `WO-O4O-KPA-TABLET-CORNER-CURRENT-PREVIEW-COMPACT-V1`
> 성격: 프론트 UI 정비 — 코너 관리 화면 안내/요약 압축 + 현재 화면 read-only 모달.
> Date: 2026-07-16

---

## 0. 결론

**PASS.** 코너 관리(`TabletCornerContentsPanel`)의 큰 안내 박스 + 별도 현재 사용 중 카드를 제거하고, 제목 줄에서 현재 적용을 확인 + `현재 화면 보기` read-only 모달로 실제 태블릿 화면을 본다. **API·DB 무변경.**

## 1. 변경 (`TabletCornerContentsPanel.tsx`)

- **제거**: 큰 주황색 안내 박스, 별도 `현재 사용 중` 카드(블록 수 요약 포함), 긴 설명 문구.
- **제목 줄**: `이 코너의 콘텐츠 (N) · 현재 적용 {0|1}개` + **[현재 화면 보기]** + [기존 콘텐츠 추가].
- **[현재 화면 보기]**: 기존 `handlePreview(current)` 재사용 → `TabletKioskPage embedded` read-only 모달(태블릿/QR 모바일 전환). 별도 페이지 이동 없음.
- **적용 해제**: 현재 카드 제거로 사라지므로 **현재 콘텐츠 kebab 으로 이관**(기능 보존, 정책 confirm). `handleClear` 의 중복 `window.confirm` 제거.

## 2. 유지 / 제거 대비

| 유지 | 제거 |
|------|------|
| 연결 5개 목록 · 현재 사용 중 배지 · 템플릿 배지 | 큰 주황색 안내 박스 |
| 이 화면 사용 · 코너에서 제거 · 순서 변경 | 별도 현재 적용 요약 카드 |
| 기존 API·DB 계약 | 진열/블록 수 강조·긴 설명 |

## 3. 실패 기준 대비 (배포본 실측)

| 실패 기준 | 결과 |
|-----------|:----:|
| 안내 박스가 별도 섹션으로 남음 | ✅ 없음 (`안내박스잔존=0`) |
| 현재 화면 확인에 다른 페이지 이동 필요 | ✅ 모달로 확인 |
| 제목 줄보다 별도 요약 카드가 더 큼 | ✅ 요약 카드 제거 |
| 연결 목록 사라짐 | ✅ 5개 유지 |
| 현재 사용 중 콘텐츠 식별 어려움 | ✅ 목록의 `● 현재 사용 중` 배지 + 제목 줄 `현재 적용 1개` |

## 4. 검증

피부 코너(연결 5).

| 항목 | 결과 |
|------|------|
| 연결 콘텐츠 5개 유지 | ✅ (기본/상품집중/제품진열/코너소개/대기영상) |
| 현재 적용 1개 표시 | ✅ 제목 줄 `현재 적용 1개` |
| 현재 화면 보기 → 모달 | ✅ 실제 태블릿 화면 렌더(헤더 QR·제품·콘텐츠), 태블릿/QR 전환 |
| 모달 닫은 뒤 목록 유지 | ✅ |
| PC(1440)·390px 제목/버튼 잘림 | ✅ 없음 (헤더 `flex-wrap`) |
| console/pageerror/API 오류 | ✅ 0 |
| `tsc` / `vite build` | ✅ EXIT=0 |
| API·DB 변경 | 없음 |

> 미리보기 모달의 상품 표시는 preview 리졸버 한계로 실제 코너 진열과 다를 수 있음(기존 preview 동작, 이 WO 범위 밖).

---

*코너 관리 = 안내박스·현재카드 제거, 제목 줄에서 현재 적용 표시 + [현재 화면 보기] read-only 모달(handlePreview 재사용). 적용 해제는 현재 kebab 으로 이관. 연결 5·배지·순서·이 화면 사용·코너에서 제거 유지. API·DB 무변경. PC·390px 실측 잘림 0.*

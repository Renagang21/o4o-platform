# CHECK-O4O-COSMETICS-GUIDE-GAP-ENRICHMENT-FULL-V1

| 항목 | 값 |
|------|-----|
| WO | `WO-O4O-COSMETICS-GUIDE-GAP-ENRICHMENT-FULL-V1` |
| 판정 | **PASS** — 결손 17,157건 전수 판정, 근거 확보분 3,297건 전량 적용, postVerify 불변식 위반 0 |
| 검증일 | 2026-08-11 |
| 시작 HEAD | `a8a1e2cd3` (작업트리 clean) |
| 선행 | `WO-O4O-COSMETICS-PRODUCTMASTER-STORE-DESCRIPTION-FULL-APPLY-V1` · `WO-O4O-COSMETICS-KO-GUIDE-FULL-PRODUCTION-V1` |
| 산출물 | `tmp/cosmetics-guide-gap-enrichment/` ([README](../../tmp/cosmetics-guide-gap-enrichment/README.md)) |
| 운영 DB write | 설명서 본문 3,297 · 유형 태그 155 · 배치 표식 3,297 (합계 UPDATE 6,749) |
| rollback | `tmp/cosmetics-guide-gap-enrichment/rollback.sql` · `tags->>'enrichBatch' = 'cosmetics-gap-enrichment-v1'` |

---

## 1. 결손 census (WO §2) — 과거 수치를 그대로 쓰지 않았다

운영 DB 32,674건을 다시 dump 해 본문에서 직접 셌다. WO 본문이 인용한 수치(결손 16,870 · `mainFeatures` 11,500 ·
`TYPE_NAME_MISMATCH` 8,079)는 선행 apply 시점 값이고, 이후 이름 정비 WO 가 지나가 **현재 값은 다르다.**

| 항목 | WO 인용 | 이번 실측 |
|------|------:|------:|
| 결손 **문서** 수 | 16,870 | **17,157** |
| `mainFeatures` 결손 | 11,500 | **11,681** |
| `usage` 결손 | 104 | **109** |
| `productType` 결손 | 38 | **42** |
| `TYPE_NAME_MISMATCH` | 8,079 | **8,233** |

**문서 수와 issue 수는 다르다** (WO §2). 결손 문서 17,157 / issue 20,254.

issue 유형별 — `NO_OBSERVED_FEATURE` 11,681 · `TYPE_NAME_MISMATCH` 8,233 · `NAME_TOO_SHORT` 174 ·
`PRODUCT_TYPE_AXIS_NOT_FORM` 67 · `NAME_EQUALS_TYPE` 57 · `PRODUCT_TYPE_UNDETERMINED` 26 · `TYPE_NAME_CONTRADICTION` 16.

결손 조합별 문서 수 — `mainFeatures` 단독 11,623 · issue 만 있고 필드 결손 없음 5,425 · `usage` 단독 28 ·
`usage+productType` 23 · `mainFeatures+usage` 39 · 셋 다 19.

**본문 무결성 실측**(`render-integrity.json`) — 선행 설명서 객체를 다시 렌더하면 32,335건은 운영 본문과 **바이트 단위로 같고**
337건은 다르다(이후 상품명 정비 WO 가 `[20개부터 구매가능]` 류 판촉 대괄호를 지운 결과). 그래서 이번 작업은 **전체 재렌더를 하지 않고**
운영 본문 블록을 그대로 두고 결손 절만 끼워 넣는 **부분 편집** 방식을 썼다.

## 2. 추가 원천 확보 (WO §3) — 7개 시도, 1개만 살아 있다

| 원천 | 결과 | 실측 근거 |
|------|------|-----------|
| **식약처 기능성화장품 보고 상세** | **사용** | 상세 1,289건 수집 · 실패 0 · 429 0 · 221초. 효능효과 1,287 / 용법용량 1,282 |
| 무신사 뷰티 상품 상세 | 사실값 없음 | 특징 결손 **100건 표본에서 고시 표 확보 0건(0%)**. 상세가 이미지라 텍스트 자체가 없다. 고시 API 는 400/404 |
| 화해 상품 상세 | 차단 | 상품 페이지·`_next/data` 모두 **HTTP 202 + 본문 0바이트** |
| 올리브영 국내몰 | 차단 | `getGoodsDetail.do` · `getSearchMain.do` **403** |
| 올리브영 글로벌 | 차단 | 검색 **403** |
| 글로우픽 | 차단 | 검색 **403** |
| 네이버 쇼핑 | 차단 | **418** |

차단은 **우회하지 않았다** (WO §3). 사설 API 역설계도 하지 않았다.
남은 축은 WO §3 우선순위 4 — **기존 census 원문 중 선행 생산이 놓친 사실값**이다.

## 3. 제품 매칭 (WO §4)

식약처 보고와의 연결은 정규화 제품명 **완전일치**만 인정했다. 부분일치·유사도 매칭은 하지 않았다.

| 판정 | 수 | 규칙 |
|------|---:|------|
| MATCH | **1,317** | `BRAND_PREFIXED_NAME` 1,119 · `NAME_EXACT` 198 |
| CHECK | 0 | 이름은 같은데 업체가 갈리는 건 없었다 |
| NO_SOURCE | 15,840 | 어떤 키로도 보고가 없다 |

업체가 여럿이면 **브랜드와 정합한 업체 1곳으로 좁혀질 때만** 연결하고, 아니면 연결하지 않는 규칙을 넣었다.

## 4. 보완 내용 (WO §5 · §7)

추가한 문장은 두 축뿐이며 **둘 다 이미 게시된 사실**이다. 성분에서 효능을 추론하거나 마케팅 카피를 옮기지 않았다.

| 축 | 근거 태그 | 특징 문장 수 |
|----|-----------|---:|
| 식약처 보고 **효능·효과** (원문 그대로) | `MFDS_REPORT_OFFICIAL` | 1,315 |
| 판매명 **괄호 안 단독 용량** 표기 | `RETAIL_LISTING` | 610 |
| 판매명 **구성 수**(`N종`·`택N`) | `RETAIL_LISTING` | 949 |
| 판매명 **수량 단위 확장**(`10매입` 등) | `RETAIL_LISTING` | 320 |

용량 두 규칙은 선행 생산의 **결함 교정**이다.
① 괄호를 통째로 지우느라 `마스크 [20g]` 의 20g 을 못 읽었다 — 괄호 안이 **용량 표기만**이고 증정어가 없을 때만 읽도록 했다.
② 수량 단위 정규식이 `10매입` 의 `입` 에 걸려 실패했다 — `매입`·`개입`·`장입` 등 실측 확인 단위를 올렸다.
증정·사은품 구간을 자르는 선행 규칙(G5)은 **그대로 유지**했다.

`usage` 는 **절이 아예 없는 건에만** 넣었다(13건, 식약처 용법용량). 이미 있는 안내를 갈아끼우지 않았다(WO §7).

## 5. `TYPE_NAME_MISMATCH` 재분류 (WO §6) — 8,233건을 일괄 수정하지 않았다

| 분류 | 수 | 처리 |
|------|---:|------|
| `DIFFERENT_AXIS` | 5,325 | 사람 확인 (CHECK) |
| `PARTIAL_TOKEN_FP` | 1,398 | **오탐** — 더 긴 낱말로 읽으면 카테고리와 같다 (`선스틱`→`스틱`, `팩트`→`팩`). 수정 불필요 |
| `SET_OR_KIT` | 743 | 세트·기획 구성이라 제형이 하나가 아니다. 수정하지 않음 |
| `SYNONYM_FP` | 612 | `마스크`/`팩` 은 같은 것의 표기 차이다. 수정 불필요 |
| `FAMILY_CORRECTION` | **155** | **수정함** — 같은 계열 안에서 상품명이 다른 제형을 명시한 건 |

수정한 155건은 `클렌징워터→클렌징밤` 59 · `바디로션→바디오일` 21 · `헤어에센스→헤어미스트` 18 ·
`클렌징폼→클렌징밤` 17 · `클렌징오일→클렌징밤` 13 등이다. 앞머리(부위·용도) 표기가 유지되므로 제품 의미가 약해지지 않는다.

**채택하지 않은 규칙** — 처음에는 판매 마디 토큰만으로 좁히는 규칙을 넣어 223건이 걸렸는데, 실측해 보니
`바디로션→크림`(99) · `트리트먼트→팩`(44) 처럼 **부위 표기가 사라지는 열화**가 대부분이었다. 규칙을 버리고
계열 일치 조건으로 바꿨다. 선행 WO 가 넓은 이름 기반 재분류를 기각한 것과 같은 결론이다.

## 6. dry-run (WO §8)

| 판정 | 수 | 비율 |
|------|---:|---:|
| **AUTO_ENRICH** | **3,297** | 19.2% |
| CHECK | 4,471 | 26.1% |
| NO_SOURCE | 8,128 | 47.4% |
| NO_CHANGE (오탐 해소) | 1,261 | 7.3% |

AUTO_ENRICH 변경 필드 — `mainFeatures` 3,145 · `productType` 155 · `usage` 13.
각 건에 `masterId / brand / productName / changedFields / before / after / source / matchEvidence` 를 남겼다.

CHECK 사유 — `TYPE_NAME_MISMATCH` 미해소 4,284 · `NAME_TOO_SHORT` 129 · `PRODUCT_TYPE_AXIS_NOT_FORM` 55 ·
`NAME_EQUALS_TYPE` 34 · `PRODUCT_TYPE_UNDETERMINED` 24 · `TYPE_NAME_CONTRADICTION` 16.

## 7. 품질 검증 (WO §9) — 위반 0, 검증기 음성 대조까지 확인

`07-validate.mjs` 는 보완 엔진을 **import 하지 않는다.** 허용 동작을 검증기에서 독립적으로 다시 유도했다. 검사 항목:

기존 블록·기존 특징 항목 소실 · 절 중복 · 본문 축소 · 꼬리(`분류`/`유통 확인`) 뒤 절 삽입 ·
**다른 제품 연결**(보고 제품명 ↔ 상품명 정규화 동일성) · **효능 문장이 보고 원문과 축자 일치하는가** ·
**용량 숫자가 판매명에 실제로 있는가** · 과장·단정 표현 · 의약품적 표현 · 유형 정정이 이름과 맞는가 ·
유형 정정이 부위 표기를 잃는가 · 기존 사용 안내 덮어쓰기.

**3,297건 위반 0.**

"위반 0" 이 검증기가 아무것도 못 잡는 상태가 아님을 **음성 대조**로 확인했다(`negative-control.mjs`).
결함 4종(기존 특징 삭제 · 효능 문장 변조 · 판매명에 없는 용량 · 부위 표기 잃는 유형 변경)을 주입하니
`ORIGINAL_FEATURE_LOST` / `MFDS_EFFICACY_NOT_VERBATIM` / `HYPE_EXPRESSION` / `NOT_A_SUPERSET` /
`CAPACITY_NUMBER_NOT_IN_NAME` / `TYPE_CHANGE_NOT_IN_NAME` / `TYPE_CHANGE_DEGRADES` 7건이 전부 잡혔다.

계통표본 100건(`index = floor(k × 3297 / 100)`, 유명 제품 고르기 금지)을 육안 확인했다 — 반복 오류군 없음.

## 8. apply (WO §10)

`content` 가 dry-run 이 본 값과 **정확히 같을 때만** 걸리는 낙관적 동시성 조건을 모든 UPDATE 에 붙였다.
100건 단위 트랜잭션.

| 항목 | 값 |
|------|---:|
| 본문 UPDATE | 3,297 |
| 유형 태그 UPDATE | 155 |
| 배치 표식 UPDATE | 3,297 |
| **UPDATE 성공** | **6,749** |
| 조건 불일치로 건너뜀 | **0** |
| 오류 | 0 |
| 소요 | 45초 |

CHECK 4,471 · NO_SOURCE 8,128 은 **미수정**이다. ProductMaster 신규 생성·삭제 0건.

## 9. postVerify (WO §11) — 적용 후 DB 를 다시 읽어 대조

| # | 검증 | 결과 |
|---|------|------|
| 1 | COSMETIC ProductMaster | **32,674 불변** |
| 2 | KO STORE canonical | **32,674 불변** |
| 3 | expected == actual (계획 3,297건 본문 일치) | **3,297 / 3,297** |
| 4 | 계획하지 않은 29,377건 본문 | **한 글자도 안 바뀜** |
| 5 | canonicalDup | **0** |
| 6 | orphan 설명서 / 설명서 없는 master | **0 / 0** |
| 7 | 본문 0자 | **0** |
| 8 | 유형 정정 반영 | 155 / 155 |
| 9 | 배치 표식 | 3,297 |
| 10 | DRUG 177,413 · 건강기능식품 40,948 · QUASI_DRUG 17,148 · MEDICAL_DEVICE 3,826 | **drift 0** |

### 결손 before → after

| 항목 | before | 보완 | after |
|------|------:|----:|------:|
| `mainFeatures` 결손 | 11,681 | **1,934** | **9,747** |
| `usage` 결손 | 109 | 13 | 96 |
| `productType` 결손 | 42 | 0 | 42 |
| `TYPE_NAME_MISMATCH` | 8,233 | 오탐 해소 2,753 + 정정 155 | 미해소 **5,325** |
| 주요 특징 절 보유 문서 | 20,993 | — | **22,927** |
| 식약처 효능·효과 보유 문서 | 0 | — | **1,315** |

`mainFeatures` 보완 1,934 < AUTO_ENRICH 3,297 인 이유 — 나머지 1,363건은 특징 절이 **이미 있는** 문서에
식약처 효능·효과를 덧붙인 건이다(결손 해소가 아니라 사실 보강).

`productType` 결손 42는 그대로다. 이름·카테고리 어느 쪽에서도 유형이 나오지 않아 **추정하지 않고 비워 뒀다.**

## 10. 효율 (WO §12)

| 항목 | 값 |
|------|-----|
| 조사 결손 문서 | 17,157 |
| 시도한 외부 원천 / 사용 가능 | 7 / **1** |
| 식약처 매칭률 | 1,317 / 17,157 = **7.7%** |
| AUTO_ENRICH률 | **19.2%** · CHECK 26.1% · NO_SOURCE 47.4% · NO_CHANGE 7.3% |
| 외부 요청 | 약 1,499회 (상세 1,289 + 접근성 탐침 210) |
| 총 처리시간 | **275초** (파이프라인 9 + 상세 수집 221 + apply 45) |
| **외부 LLM 호출** | **0건** |
| 실비 | **0원** |
| 결손 1,000건당 | 16초 · 외부 요청 87회 · 0원 |

**판단** — 병목은 처리 속도가 아니라 **원천 부재**다. 남은 `NO_SOURCE` 8,128건은 같은 방법을 반복해도 줄지 않는다.
소매 상세는 전부 차단되거나 이미지라, 더 줄이려면 **브랜드·책임판매업자 공식 자료 수급 경로**가 있어야 한다.

## 11. 관측된 후속 과제 (중지 사유 아님)

1. **식약처 상세를 이미 확보한 1,289건의 미사용분** — `사용상의주의사항` 1,282건과, 이미 사용 안내가 있는
   제품의 **제품 고유 용법용량**을 아직 쓰지 않았다. 이번 WO 는 "결손 필드 보완" 범위를 지켜 넣지 않았다.
   기존 일반 안내를 공식 용법으로 올리는 판단은 별도 WO 가 필요하다.
2. **결손 아닌 문서의 식약처 효능·효과** — 선행 census 가 매칭한 4,259건 중 결손 모집단 밖 건은 이번에 손대지 않았다.
3. `TYPE_NAME_MISMATCH` 미해소 5,325건 — 자동 규칙으로는 더 못 줄인다. 판매 카테고리 자체가 축을 섞어 놓은 경우가 많다.
4. `NON_COSMETIC_SUSPECT` 는 선행 apply 단계에서 이미 제외돼 이번 모집단에 없다.

## 12. WO 제약 준수

| 제약 | 결과 |
|------|------|
| §2 과거 수치 재사용 금지 | 준수 — 운영 DB 재측정, 차이를 표로 명시 |
| §3 차단 우회 금지 | 준수 — 403/202/418 전부 기록만 하고 중단 |
| §4 애매하면 자동 연결 금지 | 준수 — 완전일치만, 업체 불일치는 미연결 |
| §5 성분→효능 추론 금지 | 준수 — 효능 문장은 보고 원문 축자 인용만 |
| §6 8천여 건 일괄 수정 금지 | 준수 — 155건만 수정, 열화 규칙은 실측 후 폐기 |
| §7 전면 재작성 금지 | 준수 — 부분 편집, 계획 외 29,377건 바이트 동일 |
| §10 CHECK·NO_SOURCE 미수정 | 준수 |
| §10 ProductMaster 생성·삭제 금지 | 준수 — 0건 |
| §12 불필요한 외부 LLM 금지 | 준수 — 0건 |

## 13. 문서 정합

문서 정합: 발견 0건 / SUPERSEDED 표기 0건 / 링크 수정 0건 / 별도 WO 제안 0건

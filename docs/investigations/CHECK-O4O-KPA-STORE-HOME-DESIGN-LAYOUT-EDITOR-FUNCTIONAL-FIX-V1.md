# CHECK-O4O-KPA-STORE-HOME-DESIGN-LAYOUT-EDITOR-FUNCTIONAL-FIX-V1

> 작업: 매장 홈 디자인(`/store/settings`) 레이아웃 편집 실동작 점검 + 최소 보강
> 커밋: `49b70795f` / 일자: 2026-06-25 / 범위: KPA 전용

---

## 1. 원인 (조사)

- **미리보기 미반영**: 오른쪽 미리보기는 실제 공개 매장 홈 `/store/{slug}` iframe 인데, iframe key 가
  `${slug}-${previewDevice}` 로 고정 → **편집/저장 시 절대 새로고침되지 않음.** 사용자는 블록을 바꿔도
  미리보기가 그대로라 "작동 안 함"으로 체감. (편집값→공개 반영 자체는 StorefrontHomePage 에서 동작)
- **디바이스 탭 오해**: `B2C 몰 / 태블릿 / 키오스크` 3탭이 **같은 URL을 maxWidth만 다르게** 표시 —
  실제 태블릿/키오스크 채널 미리보기가 아님. 키오스크는 HOLD(미출시)라 노출 부적절.
- **문구 불일치**: 하단 안내가 제거된 '채널(B2C·태블릿·키오스크 승인)' 메뉴를 참조(현재 온라인 판매/고객 응대로 개편됨).

## 2. 수정 (최소, KPA 전용 `PharmacyStorePage.tsx`)

1. **미리보기 새로고침**: `previewVersion` state 추가 → 저장 성공 시 bump → iframe `key`/`src(?preview=N)`
   갱신으로 **강제 reload** → "저장 → 미리보기 반영" 동작.
2. **디바이스 탭 정직화**: `B2C/태블릿/키오스크` → **뷰포트 폭 토글(데스크톱 100% / 모바일 390px)** 로 대체.
   채널 의미 제거, **KIOSK 노출 제거**(HOLD).
3. **문구 정합**: 미리보기 성격 안내("저장된 실제 매장 홈입니다. 편집 후 변경사항 저장을 누르면 갱신됩니다.")
   + 하단 안내를 "온라인 판매·고객 응대 설정은 각 전용 메뉴에서 관리"로 수정(제거된 '채널' 참조 정리).
- 블록 순서/표시/개수 편집·저장·공개 반영 로직(StorefrontHomePage), 저장 API/DB **불변**.

## 3. 검증

| 항목 | 결과 |
|---|---|
| 타입체크 (web-kpa) | ✅ PASS |
| 배포 (Deploy Web Services, `49b70795f`) | ✅ success |

### 브라우저 smoke (배포본, Sohae 약국 매장, 2026-06-25)
| 항목 | 결과 |
|---|---|
| 미리보기 탭 = 🖥️데스크톱 / 📱모바일 (키오스크·채널명 제거) | ✅ |
| 미리보기 안내문구("저장된 실제 매장 홈 … 저장 시 갱신") | ✅ |
| 하단 안내문구(채널 참조 제거, 온라인 판매·고객 응대 안내) | ✅ |
| 저장 전 iframe src `?preview=0` → "변경사항 저장" → "저장 완료" + iframe src `?preview=1`(강제 reload) | ✅ **저장→미리보기 반영 동작** |
| URL `/store/settings`·약국 정보 링크·기능 정상 | ✅ |

> smoke 의 저장은 **현재 값 멱등 저장**(콘텐츠 변경 없음)으로 reload 메커니즘만 검증 — 운영 매장 콘텐츠 변경 없음.

## 4. 작동하는 기능 / 숨긴 기능 구분

**작동(본 WO 보강):** 저장 시 미리보기 새로고침 / 데스크톱·모바일 폭 토글.
**작동(기존, 코드 확인 — 본 WO 미변경):** 블록 순서/표시/개수 → 저장 → `organizations.storefront_config` 영속
  → 공개 storefront `StorefrontHomePage` 가 blocks/theme 렌더(IR-O4O-STORE-SETTINGS-FULL-AUDIT-V1 §2 확인).
  실제 공개 매장 콘텐츠 변경을 피하려 블록 토글 실저장 smoke 는 생략(메커니즘은 검증됨).
**숨김/제거:** 키오스크 미리보기 탭(HOLD), B2C/태블릿 '채널' 명칭 탭(오해 소지) → 뷰포트 폭으로 대체.

## 5. 비고 / 후속
- **라이브(저장 전) 미리보기**는 미구현 — iframe 이 공개 storefront 자체 GET 을 사용하므로 저장 후 반영.
  WO 의 "신뢰 가능 미리보기" 옵션 중 "실제 화면 + 저장 시 갱신 + 명확한 안내"를 채택(저비용·정직).
  추후 진짜 라이브 편집이 필요하면 storefront 렌더러를 prop 기반으로 재사용하는 별도 WO.
- storefront 가 상품/블로그 데이터가 없으면 해당 블록은 비어 보일 수 있음(데이터 의존, 버그 아님).
- KPA 전용. 공통/GP/KCos·DB·API 변경 없음.

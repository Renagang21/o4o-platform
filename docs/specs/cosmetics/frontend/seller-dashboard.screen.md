# Seller Dashboard - 화면 정의서

> **Work Order**: WO-01-SELLER-DASHBOARD-SCREEN
> **Status**: FINAL (운영 기준 반영 완료)
> **Date**: 2025-12-17
> **Design Core**: v1.0 필수 적용

---

## 1. 문서 목적

이 문서는 **Seller Dashboard 화면을 프론트엔드가 그대로 구현할 수 있는 정의서**이다.

- 운영 시나리오 정의서의 UI 번역본
- 개발자가 질문 없이 구현 가능한 수준으로 작성
- **추가 해석/변형 금지**

---

## 2. 화면 구조

```
┌─────────────────────────────────────────────────────────────┐
│  [AGPageHeader]                                              │
│  제목: "판매원 대시보드"                                      │
│  설명: {오늘 날짜}                                           │
│  Actions: [기간 필터 버튼] [새로고침]                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  [AGSection] 주요 KPI                                        │
│  ┌──────────┬──────────┬──────────┬──────────┐              │
│  │ 총 상담   │ 전환율    │ 진열 수   │ 재고 부족 │              │
│  │ {value}  │ {value}% │ {value}  │ {value}  │              │
│  │ info     │ positive │ neutral  │ dynamic  │              │
│  └──────────┴──────────┴──────────┴──────────┘              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  [AGSection] 샘플 성과 ({기간 레이블})                        │
│  ┌──────────┬──────────┬──────────┬──────────┐              │
│  │ 총 재고   │ 샘플 사용 │ 샘플→구매 │ 재고 부족 │              │
│  │ {value}  │ {value}  │ {value}% │ {value}  │              │
│  │ {N}종    │ {N}개 제품│ {N}건 구매│ 품절:{N} │              │
│  └──────────┴──────────┴──────────┴──────────┘              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  [AGSection] 진열 건강도                                     │
│  ┌──────────┬──────────┬──────────┬──────────┐              │
│  │ 건강 점수 │ 총 진열   │ 인증 완료 │ 미인증    │              │
│  │ {value}% │ {value}  │ {value}  │ {value}  │              │
│  │ dynamic  │ info     │ positive │ dynamic  │              │
│  └──────────┴──────────┴──────────┴──────────┘              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  [AGSection] 진열 현황                                       │
│  [AGCard 3열 그리드]                                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  [AGSection] 최근 상담                                       │
│  [AGTable]                                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 컴포넌트 상세 정의

### 3.1 기간 필터

| 속성 | 값 |
|------|-----|
| 컴포넌트 | `Button` (3개 그룹) |
| 옵션 | `오늘` / `이번 주` / `이번 달` |
| 기본값 | `오늘` |
| 동작 | 선택 시 샘플 성과 섹션 데이터 갱신 |

```tsx
// 허용되는 기간 값 (확장 금지)
type PeriodType = 'daily' | 'weekly' | 'monthly';
```

### 3.2 KPI 카드 색상 규칙 (강제)

| colorMode | 조건 | 색상 |
|-----------|------|------|
| `positive` | 정상 상태 | 🟢 녹색 |
| `neutral` | 관찰 필요 | ⚪ 회색 |
| `negative` | 즉시 조치 | 🔴 적색 |
| `info` | 정보성 (판단 불필요) | 🔵 청색 |

### 3.3 동적 색상 판정 규칙

#### 전환율

```tsx
colorMode = conversionRate >= 15 ? 'positive'
          : conversionRate >= 10 ? 'neutral'
          : 'negative';
```

#### 건강 점수

```tsx
colorMode = healthScore >= 80 ? 'positive'
          : healthScore >= 50 ? 'neutral'
          : 'negative';
```

#### 재고 부족

```tsx
colorMode = lowStockCount === 0 ? 'neutral' : 'negative';
```

#### 미인증 진열

```tsx
colorMode = unverifiedDisplays === 0 ? 'neutral' : 'negative';
```

---

## 4. KPI 카드 상세

### 4.1 주요 KPI 섹션

| 카드 | title | value | colorMode | subtitle |
|------|-------|-------|-----------|----------|
| 1 | "총 상담" | `{totalConsultations}` | `info` | - |
| 2 | "전환율" | `{conversionRate}%` | **동적** | - |
| 3 | "진열 수" | `{totalDisplays}` | `neutral` | - |
| 4 | "재고 부족" | `{lowStockCount}` | **동적** | - |

### 4.2 샘플 성과 섹션

| 카드 | title | value | colorMode | subtitle |
|------|-------|-------|-----------|----------|
| 1 | "총 재고" | `{totalQuantity}` | `info` | `{totalItems}종` |
| 2 | "샘플 사용" | `{totalUsage}` | `neutral` | `{uniqueProducts}개 제품` |
| 3 | "샘플→구매 전환" | `{conversionRate}%` | `positive` | `{purchaseCount}건 구매` |
| 4 | "재고 부족" | `{lowStockCount}` | **동적** | `품절: {outOfStockCount}` |

### 4.3 진열 건강도 섹션

| 카드 | title | value | colorMode | subtitle |
|------|-------|-------|-----------|----------|
| 1 | "건강 점수" | `{healthScore}%` | **동적** | - |
| 2 | "총 진열" | `{totalDisplays}` | `info` | `활성: {activeDisplays}` |
| 3 | "인증 완료" | `{verifiedDisplays}` | `positive` | - |
| 4 | "미인증" | `{unverifiedDisplays}` | **동적** | - |

---

## 5. API 연결 매핑

### 기본 데이터 (페이지 로드 시)

| 데이터 | API Endpoint |
|--------|--------------|
| Display Stats | `GET /api/v1/cosmetics-seller/display/seller/{sellerId}/stats` |
| Sample Stats | `GET /api/v1/cosmetics-seller/sample/seller/{sellerId}/stats` |
| Inventory Stats | `GET /api/v1/cosmetics-seller/inventory/seller/{sellerId}/stats` |
| Consultation Stats | `GET /api/v1/cosmetics-seller/consultation/seller/{sellerId}/stats` |
| Recent Consultations | `GET /api/v1/cosmetics-seller/consultation/seller/{sellerId}?limit=5` |

### 확장 KPI 데이터 (기간 필터 연동)

| 데이터 | API Endpoint |
|--------|--------------|
| Sample Inventory Stats | `GET /api/v1/cosmetics-sample/inventory/{storeId}/stats` |
| Sample Usage | `GET /api/v1/cosmetics-sample/usage/{storeId}/daily?days={N}` |
| Sample Analytics | `GET /api/v1/cosmetics-sample/analytics/overall` |
| Display Summary | `GET /api/v1/cosmetics-sample/display/{storeId}/summary` |

### 기간별 days 파라미터

| 기간 | days |
|------|------|
| 오늘 | 1 |
| 이번 주 | 7 |
| 이번 달 | 30 |

---

## 6. 금지 사항 (명시적)

다음은 **이 화면에서 절대 허용되지 않는다**:

| 금지 항목 | 사유 |
|----------|------|
| 새로운 KPI 카드 추가 | 운영 기준 고정됨 |
| 색상 규칙 변경 | Design Core 강제 규칙 |
| 기간 필터 옵션 추가 | 운영 표준 고정 |
| 그래프/차트 추가 | 운영 범위 외 |
| 상세 페이지 링크 추가 | 별도 Work Order 필요 |
| 다국어 변환 | 현재 범위 외 |

---

## 7. 구현 체크리스트

프론트엔드 개발자는 다음을 확인한다:

- [ ] Design Core v1.0 컴포넌트만 사용
- [ ] AGPageHeader, AGSection, AGKPIGrid, AGKPIBlock 사용
- [ ] 기간 필터 3개 옵션만 존재
- [ ] 색상 판정 로직 정확히 구현
- [ ] API 연결 완료
- [ ] 에러 시 기본값 0 표시 (UI 깨짐 방지)

---

## 8. 관련 문서

- [운영 시나리오 정의서](../seller-operation-scenarios.md)
- [KPI 빠른 해석 가이드](../seller-kpi-quick-guide.md)
- [Design Core KPI 규칙](./design-core-kpi-rules.md)

---

*이 문서는 운영 기준의 UI 번역본이며, 추가 해석 없이 그대로 구현한다.*

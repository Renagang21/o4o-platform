# WO-SERVICE-PARTNER-DASHBOARD-ROUTING-MENU-V1

**(서비스별 파트너 대시보드 라우팅 & 메뉴 연결 정의)**

---

## 1. Work Order 목적 (Purpose)

본 Work Order는 다음을 목적으로 한다.

* 서비스별 파트너 대시보드의 **URL 진입 경로를 명확히 고정**한다.
* 메뉴 위치·진입 방식·접근 권한을 통일하여
  **서비스 간 구조 일관성**을 확보한다.
* 이후 프론트엔드/백엔드 구현 시
  **"경로·메뉴·진입점"에 대한 재논의를 제거**한다.

👉 본 문서는 **구현 이전의 마지막 구조 고정 단계**이다.

---

## 2. 적용 범위 (Scope)

### 적용 대상

* 모든 서비스 내부 파트너 대시보드

  * GlycoPharm
  * K-Cosmetics
  * GlucoseView
  * 향후 추가 서비스

### 적용 제외

* Neture (파트너 안내 허브)
* 관리자(Admin)
* 공급자(Supplier)
* 판매자(Seller)

---

## 3. 상위 기준 문서 (Normative References)

본 Work Order는 아래 문서를 절대 기준으로 한다.

* **WO-NETURE-PARTNER-DASHBOARD-RESPONSIBILITY-V1**
* **WO-SERVICE-PARTNER-DASHBOARD-BASELINE-V1**
* **WO-SERVICE-PARTNER-DASHBOARD-UI-SKELETON-V1**

---

## 4. 파트너 대시보드 기본 URL 구조 (Routing Baseline)

### 최상위 진입 경로

모든 서비스는 다음 경로를 사용한다.

```
/partner
```

* 서비스 도메인 내부 경로이다.

  * 예:

    * glycopharm.co.kr/partner
    * k-cosmetics.site/partner
    * glucoseview.co.kr/partner

* 별도 서브도메인 사용 (X)

* Neture 경유 (X)

---

### 4.1 하위 라우트 표준

```
/partner
 ├─ /overview        (요약)
 ├─ /targets         (홍보 대상)
 ├─ /content         (콘텐츠)
 ├─ /events          (이벤트 조건)
 └─ /status          (상태)
```

다음 라우트는 **존재할 수 없다**:

* `/partner/approval`
* `/partner/analytics`
* `/partner/admin`
* `/partner/settings`

---

## 5. 메뉴 연결 기준 (Menu Placement Rules)

### 5.1 글로벌 네비게이션 기준

파트너 대시보드는 다음 위치 중 **하나만** 허용한다.

### 허용 위치

* 로그인 후 사용자 메뉴(User Menu)

  * "파트너 대시보드"
* 서비스 전용 사이드 메뉴

  * 역할별 섹션 하위

### 금지 위치

* 관리자 메뉴 하위
* 공급자/판매자 메뉴 내부
* 메인 서비스 CTA 영역
* Neture 공통 메뉴

---

### 5.2 메뉴 노출 조건

메뉴는 **다음 조건을 모두 만족할 때만 노출**된다.

* 현재 사용자가 `partner` 역할을 보유
* 해당 서비스에 **연결된 파트너 관계가 존재**
* 서비스가 파트너 기능을 활성화한 상태

메뉴는 다음 경우 숨김 처리한다:

* 파트너 관계가 종료됨
* 서비스 접근 권한이 없음

---

## 6. 접근 제어 및 권한 가드 (Access Control)

### 6.1 접근 허용 기준

* `/partner/*` 모든 경로는

  * 로그인 필수
  * `partner` Role 필수

### 6.2 접근 불가 처리

* 조건 불충족 시:

  * 403 Forbidden
  * 또는 서비스 지정 "접근 불가 안내 페이지"

금지 사항:
* 자동 관리자 전환 (X)
* 승인 요청 유도 UI (X)
* Neture 이동 유도 (X)

---

## 7. 서비스별 자율 영역과 고정 영역

### 7.1 고정 영역 (변경 불가)

* `/partner` 경로
* 하위 라우트 개수 및 의미
* 메뉴 노출 조건
* 권한 가드 기준

---

### 7.2 서비스별 허용 자율 영역

* 메뉴 명칭 문구
* 메뉴 위치(UI 배치)
* 하위 라우트 내부 UI 구성
* 기본 진입 페이지(`/partner` → 어느 섹션으로 리다이렉트할지)

---

## 8. 개발 구조에 미치는 영향 (Implementation Notes)

이 Work Order 이후:

* 프론트엔드

  * `pages/partner/*` 또는 동등 구조 생성 가능
  * 라우터 설정 고정
  * 메뉴 컴포넌트 연결 기준 명확

* 백엔드

  * `/partner/*` API 그룹 분리 가능
  * 권한 가드 일관 적용 가능

👉 **UI 구현과 API 설계가 동시에 단순해진다.**

---

## 9. Work Order 종료 조건 (DoD)

다음이 충족되면 본 Work Order는 완료로 간주한다.

* 모든 서비스에서 `/partner/*` 경로 구조가 동일하게 정의됨
* 메뉴 노출 조건이 명확히 문서화됨
* 파트너 대시보드 진입점에 대한 추가 논의가 불필요해짐
* 이후 프론트엔드 구현 WO에서
  라우팅/메뉴 관련 설계가 반복되지 않음

---

## 10. 최종 기준 문장

> **파트너 대시보드는
> "역할이 있을 때만 보이고,
> 서비스 안에서만 존재한다."**

---

*Created: 2026-01-15*
*Status: Active Routing Standard*
*References: WO-SERVICE-PARTNER-DASHBOARD-UI-SKELETON-V1*

# Generator CLI 정의서 (o4o generate)

> **Status**: Active (Phase 13 확정)
> **Created**: 2025-12-29
> **Location**: `scripts/cli/o4o.ts`

---

## 1. 개요

Generator CLI는 **대화형 인터페이스로 Web/Admin 페이지 생성 파이프라인을 실행**하는 도구이다.

### 1.1 목적

| Before (수동) | After (CLI) |
|--------------|-------------|
| Generator 3개 개별 실행 | CLI 1회 실행 |
| 옵션 수동 입력 | 질문 응답형 선택 |
| 파일 구조 암기 필요 | 자동 감지 및 제안 |
| 결과 확인 수동 | 요약 리포트 자동 |

### 1.2 설계 원칙

1. **오케스트레이터 역할**: 기존 Generator를 순서대로 호출
2. **질문-응답 방식**: 사용자는 선택만, 플랫폼이 생성
3. **안전 우선**: Dry-run 및 중복 방지 내장
4. **결과 투명성**: 생성 전후 상태 명확히 표시

---

## 2. 사용법

### 2.1 기본 사용

```bash
# 인터랙티브 모드 실행
pnpm o4o:generate

# 또는 직접 실행
npx tsx scripts/cli/o4o.ts generate
```

### 2.2 옵션

| 옵션 | 설명 |
|------|------|
| `--dry-run` | 실제 파일 생성 없이 미리보기만 |
| `--force` | 기존 디렉터리 존재 시 덮어쓰기 |
| `--help` | 도움말 표시 |

```bash
# Dry-run 모드
pnpm o4o:generate:dry

# 강제 덮어쓰기
npx tsx scripts/cli/o4o.ts generate --force
```

---

## 3. 인터랙티브 플로우

### 3.1 실행 흐름

```
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Business 선택                                        │
│   - docs/services/* 자동 스캔                                │
│   - OpenAPI 존재하는 서비스만 표시                            │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Entity 선택                                          │
│   - OpenAPI paths에서 엔티티 자동 추출                        │
│   - Admin 지원 여부 표시                                      │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: 생성 옵션                                            │
│   - Web (List/Detail) 생성 여부                              │
│   - Admin (Create/Edit/Status) 생성 여부                     │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: 표시 정보                                            │
│   - Display Name (한글)                                      │
│   - Lucide Icon                                              │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 5: 미리보기 및 확인                                      │
│   - 생성될 파일 목록                                          │
│   - 실행될 Generator                                         │
│   - Yes/No 확인                                              │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 6: 생성 실행                                            │
│   Phase 1: Types (openapi-types-generator)                  │
│   Phase 2: Web (web-extension-generator)                    │
│   Phase 3: Admin (web-admin-generator)                      │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 7: 결과 요약                                            │
│   - 생성된 파일 목록                                          │
│   - 다음 단계 안내                                            │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 예시 실행

```
╔════════════════════════════════════════════════════════════╗
║          O4O Platform Generator CLI (Phase 13)             ║
╚════════════════════════════════════════════════════════════╝

📁 Business 선택:
  1. cosmetics (4 entities)
  2. yaksa (2 entities)
선택 (1-2): 1

   선택된 Business: cosmetics
   OpenAPI: docs/services/cosmetics/openapi.yaml

📦 Entity 선택:
  1. products (Admin 지원)
  2. brands
  3. lines
  4. prices (Admin 지원)
선택 (1-4): 1

📋 생성 옵션:
   Web (List/Detail) 페이지 생성? [Y/n]: y
   Admin (Create/Edit/Status) 페이지 생성? [Y/n]: y

📝 Display Name [Cosmetics Product]: 화장품 제품
🎨 Lucide Icon [Package]:

═══════════════════════════════════════════════════════════════
📋 생성 미리보기
═══════════════════════════════════════════════════════════════
   Business:     cosmetics
   Entity:       products
   Display Name: 화장품 제품
   Icon:         Package
   Web 생성:     ✅
   Admin 생성:   ✅

   실행될 Generator:
   1. openapi-types-generator.ts (Types)
   2. web-extension-generator.ts (Web List/Detail)
   3. web-admin-generator.ts (Admin Create/Edit/Status)

🚀 생성을 시작하시겠습니까? [Y/n]: y

═══════════════════════════════════════════════════════════════
🔄 생성 시작
═══════════════════════════════════════════════════════════════

📦 Phase 1: OpenAPI → Types 생성
...
📄 Phase 2: Web (List/Detail) 생성
...
🔧 Phase 3: Admin (Create/Edit/Status) 생성
...

═══════════════════════════════════════════════════════════════
✨ 생성 완료
═══════════════════════════════════════════════════════════════

📁 생성된 파일/디렉터리:
   - packages/api-types/src/cosmetics.ts
   - apps/admin-dashboard/src/pages/cosmetics-products
   - apps/admin-dashboard/src/pages/cosmetics-products-admin

📌 다음 단계:
   1. App.tsx에 Router 등록
   2. API 서버가 해당 엔드포인트를 지원하는지 확인
   3. pnpm run dev:admin으로 확인
```

---

## 4. 내부 실행 구조

### 4.1 Generator 호출 순서

| 단계 | Generator | 역할 |
|------|-----------|------|
| Phase 1 | `openapi-types-generator.ts` | TypeScript 타입 생성 |
| Phase 2 | `web-extension-generator.ts` | List/Detail 페이지 생성 |
| Phase 3 | `web-admin-generator.ts` | Create/Edit/Status 페이지 생성 |

### 4.2 실패 처리

- Phase 1 실패 → 즉시 중단
- Phase 2 실패 → 즉시 중단
- Phase 3 실패 → 오류 표시 (Types/Web은 유지)

---

## 5. 안전장치

### 5.1 Dry-run 모드

```bash
pnpm o4o:generate:dry
```

- 실제 파일 생성 없음
- 모든 질문/선택 동일하게 진행
- 생성될 파일 목록만 표시

### 5.2 중복 방지

디렉터리가 이미 존재하는 경우:

```
⚠️  이미 존재하는 디렉터리가 있습니다:
   apps/admin-dashboard/src/pages/cosmetics-products

   --force 옵션으로 덮어쓰기할 수 있습니다.
   계속 진행하시겠습니까? [y/N]:
```

- 기본값: 중단 (N)
- `--force` 옵션: 확인 없이 덮어쓰기

---

## 6. 자동 감지 기능

### 6.1 서비스 감지

```
docs/services/
├── cosmetics/
│   └── openapi.yaml  → cosmetics 서비스 감지
├── yaksa/
│   └── openapi.yaml  → yaksa 서비스 감지
└── dropshipping/
    └── openapi.yaml  → dropshipping 서비스 감지
```

### 6.2 엔티티 추출

OpenAPI paths에서 자동 추출:
- `/cosmetics/products` → products 엔티티
- `/cosmetics/admin/products` → Admin 지원 표시
- `/cosmetics/brands` → brands 엔티티

### 6.3 아이콘 제안

| 엔티티 | 기본 아이콘 |
|--------|------------|
| products | Package |
| brands | Award |
| posts | MessageSquare |
| members | Users |
| orders | ShoppingCart |
| 기타 | FileText |

---

## 7. 관련 규칙

### 7.1 CLI 우선 원칙

- **신규 페이지 생성**: CLI 사용 권장
- **재생성**: CLI 또는 개별 Generator 허용
- **수동 생성**: 금지 (§18, §19 위반)

### 7.2 생성 코드 수정

CLI로 생성된 코드도 **직접 수정 금지** 원칙 동일 적용:
- Types: OpenAPI 수정 → 재생성
- Web/Admin: Generator 입력 수정 → 재생성

---

## 8. 관련 문서

- [Web Extension Generator](./web-extension-generator.md) - Phase 10
- [Web Admin Generator](./web-admin-generator.md) - Phase 11
- [OpenAPI Types Automation](./openapi-types-automation.md) - Phase 12
- [CLAUDE.md §22](../../CLAUDE.md) - CLI Rules

---

*Created: 2025-12-29*
*Phase: 13*
*Status: Active*

/**
 * Organization-LMS Backend Entry Point
 *
 * AppStore 표준 준수를 위한 backend exports
 */

// Organization-LMS는 현재 자체 entities/services 없음
// lms-core와 organization-core를 연결하는 Extension 역할

export {};

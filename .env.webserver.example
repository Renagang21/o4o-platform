# ================================
# O4O Platform - 웹서버 환경 변수 예제
# 이 파일을 복사하여 .env.webserver로 저장하고 값을 설정하세요
# ================================

# === 서버 기본 설정 ===
NODE_ENV=production
SERVER_TYPE=webserver
PORT=3000

# === API 서버 연결 ===
VITE_API_URL=http://your-api-server:3001
API_BASE_URL=http://your-api-server:3001

# === 정적 파일 서빙 ===
STATIC_FILES_PATH=/path/to/public
UPLOAD_DIR=/path/to/uploads

# === PM2 설정 ===
PM2_APP_NAME=o4o-webserver
PM2_INSTANCES=2

# === 로그 설정 ===
LOG_LEVEL=info
LOG_DIR=/path/to/logs

# === 보안 설정 ===
CORS_ORIGIN=https://your-domain.com
SESSION_SECRET=change-this-secret-key

# === 캐시 설정 ===
CACHE_ENABLED=true
CACHE_TTL=3600

# === 모니터링 ===
MONITORING_ENABLED=false
HEALTH_CHECK_PATH=/health
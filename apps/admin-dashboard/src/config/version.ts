// Version configuration for admin dashboard
export const VERSION = '1.0.2'; // SSH key configured, ready for deployment
export const BUILD_TIME = new Date().toISOString();
export const FEATURES = {
  paragraphTestBlock: true,
  standaloneEditor: true,
  gutenbergEditor: true,
  deploymentFixed: true
};
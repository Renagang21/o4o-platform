/**
 * CPT/ACF Loop Block Save Component
 * 
 * This returns null because the block is rendered dynamically on the server side
 */

export default function save() {
  // Server-side rendered block
  return null;
}
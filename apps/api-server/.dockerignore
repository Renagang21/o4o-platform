# =============================================================================
# Docker Ignore - O4O API Server
# =============================================================================
# Only include files needed for production runtime:
# - dist/** (compiled TypeScript files)
# - package.production.json (runtime dependencies)
# =============================================================================

# Ignore everything by default
*

# Allow production files
!dist/**
!package.production.json
!package.production-minimal.json
!Dockerfile

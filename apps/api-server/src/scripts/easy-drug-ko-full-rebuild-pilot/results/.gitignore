# 본문을 담은 대용량 산출물은 저장소에 넣지 않는다 (WO §10).
# 재현 방법은 README.md 에 있고, 동일 입력이면 byte-identical 로 다시 만들어진다.
# 추적하는 것: 요약 JSON 전체 + 본문 없는 원장(hash·판정·수치)
source-snapshot.jsonl
master-census.jsonl
structured.jsonl
leaflets.jsonl

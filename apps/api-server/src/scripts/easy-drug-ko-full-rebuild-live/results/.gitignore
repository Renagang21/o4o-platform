# 본문을 담은 대용량 산출물은 저장소에 넣지 않는다 (WO §5).
# 동일 동결 snapshot 이면 byte-identical 로 다시 만들어진다 — 재현 절차는 README.md.
# 추적하는 것: 요약 JSON 전체 + 본문 없는 원장(hash·판정·수치) + LIVE write 결과.
frozen-source.jsonl
population.jsonl
leaflets.jsonl
plan-run1.jsonl
plan-run2.jsonl
plan-run3.jsonl
apply-eligible.jsonl
apply-result-rollback.jsonl

# 본문을 담은 대용량 산출물은 저장소에 넣지 않는다 (WO §5).
# 동일 동결 snapshot 이면 byte-identical 로 다시 만들어진다 — 재현 절차는 README.md.
# 추적하는 것: 요약 JSON 전체 + 본문 없는 원장(hash·판정·수치) + LIVE write 결과.
frozen-source.jsonl
population.jsonl
leaflets.jsonl
plan-run1.jsonl
plan-run2.jsonl
plan-run3.jsonl
apply-eligible.jsonl
apply-result-rollback.jsonl
# 비노출 계획 원장. 추적 중인 translation-status-ledger.jsonl + planDigest 로 재생성된다.
hide-plan.jsonl
hide-plan-run1.jsonl
hide-plan-run2.jsonl
hide-plan-rerun.jsonl
# 대용량 스크린샷 원본. 대표 증거만 ../evidence/ 로 추려 커밋한다.
screenshots/

# 전수 원장 원본(수천만 셀). 위험 하위 구간 worst500 만 커밋한다.
h1-population.jsonl
h1-layout-slack-ledger.jsonl
browser-smoke-result.json
browser-smoke-findings.jsonl
browser-smoke-rows.jsonl

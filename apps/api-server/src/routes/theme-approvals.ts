import express from 'express';

// Theme customizations feature temporarily disabled
// This file contained references to removed User.themeCustomizations field
// TODO: Implement theme customizations properly in metadata or separate entity

const router: express.Router = express.Router();

export default router;
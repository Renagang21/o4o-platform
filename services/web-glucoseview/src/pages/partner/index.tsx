/**
 * PartnerIndex - 파트너 대시보드 인덱스
 * Reference: GlycoPharm (복제)
 */

import { Navigate } from 'react-router-dom';

export default function PartnerIndex() {
  return <Navigate to="/partner/overview" replace />;
}

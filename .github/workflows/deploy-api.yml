name: Deploy API Server (Cloud Run)

# Deploy API server to Cloud Run on push to main branch
# Phase R5: Cloud Run 단일 배포 체계
# Phase G1-1: Operational 전환 준비 (Cloud SQL 연결 시 GRACEFUL_STARTUP=false로 변경)

concurrency:
  group: deploy-api-${{ github.ref }}
  cancel-in-progress: false

on:
  push:
    branches:
      - main
    paths:
      - 'apps/api-server/**'
      - 'packages/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/deploy-api.yml'
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment'
        required: false
        default: 'false'

env:
  PROJECT_ID: netureyoutube
  REGION: asia-northeast3
  SERVICE_NAME: o4o-core-api
  REPOSITORY_NAME: o4o-api
  IMAGE_NAME: api-server

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write  # Required for Workload Identity Federation

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.18.0'

    - name: Install pnpm
      run: npm install -g pnpm

    - name: Install dependencies
      run: pnpm install --frozen-lockfile

    - name: Build all packages
      run: |
        echo "🔨 Building all workspace packages..."
        # Build packages in dependency order using pnpm recursive
        pnpm -r --filter "!@o4o/admin-dashboard" --filter "!@o4o/main-site" --filter "!@o4o/ecommerce" run build || true
        echo "✅ Package builds complete"

    - name: Build API server (bundled with tsup)
      run: |
        cd apps/api-server

        # Use tsup to bundle the API server with all @o4o/* packages inline
        # This creates a single bundle that doesn't depend on workspace packages
        echo "🔨 Building API server with tsup (bundled build)..."
        pnpm run build:api

        # Verify build output
        if [ ! -f "dist/main.js" ]; then
          echo "❌ Build failed: dist/main.js not found"
          exit 1
        fi

        echo "✅ API server build complete"
        echo "📦 Bundle size: $(du -h dist/main.js | cut -f1)"

        # List generated files
        echo "📋 Generated files:"
        ls -la dist/

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: ${{ env.PROJECT_ID }}

    - name: Configure Docker for Artifact Registry
      run: |
        gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

    - name: Build and Push Docker image
      run: |
        cd apps/api-server

        # Verify tsup bundle exists
        echo "📂 Checking bundled output:"
        ls -la dist/main.js dist/main.js.map || (echo "❌ Bundle not found!" && exit 1)
        echo "📦 Bundle size: $(du -h dist/main.js | cut -f1)"

        IMAGE_TAG="${{ github.sha }}"
        FULL_IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY_NAME }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}"
        LATEST_IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY_NAME }}/${{ env.IMAGE_NAME }}:latest"

        echo "🐳 Building Docker image..."
        docker build \
          --no-cache \
          --build-arg CACHEBUST=$(date +%s) \
          --platform linux/amd64 \
          --tag "${FULL_IMAGE}" \
          --tag "${LATEST_IMAGE}" \
          --file Dockerfile \
          .

        # Verify the image contains the bundle
        echo "🔍 Verifying Docker image contents..."
        docker run --rm "${FULL_IMAGE}" ls -la /app/dist/

        echo "📤 Pushing to Artifact Registry..."
        docker push "${FULL_IMAGE}"
        docker push "${LATEST_IMAGE}"

        echo "✅ Image pushed: ${FULL_IMAGE}"
        echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_ENV

    - name: Deploy to Cloud Run
      run: |
        FULL_IMAGE="${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY_NAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}"

        echo "🚀 Deploying to Cloud Run..."

        # ============================================================
        # G1-1 Operational Mode (2025-12-25)
        # ============================================================
        # 상태: Operational - Cloud SQL 연결 활성화
        # Cloud SQL: o4o-platform-db (netureyoutube:asia-northeast3)
        # ============================================================

        gcloud run deploy ${{ env.SERVICE_NAME }} \
          --image="${FULL_IMAGE}" \
          --region=${{ env.REGION }} \
          --project=${{ env.PROJECT_ID }} \
          --platform=managed \
          --allow-unauthenticated \
          --port=8080 \
          --memory=1Gi \
          --cpu=1 \
          --min-instances=0 \
          --max-instances=10 \
          --concurrency=80 \
          --timeout=300 \
          --set-env-vars="NODE_ENV=production" \
          --set-env-vars="GRACEFUL_STARTUP=false" \
          --set-env-vars="EMAIL_SERVICE_ENABLED=false" \
          --set-env-vars="DB_HOST=/cloudsql/netureyoutube:asia-northeast3:o4o-platform-db" \
          --set-env-vars="DB_PORT=5432" \
          --set-env-vars="DB_USERNAME=${{ secrets.GCP_DB_USERNAME }}" \
          --set-env-vars="DB_PASSWORD=${{ secrets.GCP_DB_PASSWORD }}" \
          --set-env-vars="DB_NAME=${{ secrets.GCP_DB_NAME }}" \
          --set-env-vars="JWT_SECRET=${{ secrets.GCP_JWT_SECRET }}" \
          --add-cloudsql-instances=netureyoutube:asia-northeast3:o4o-platform-db

    - name: Verify deployment
      run: |
        echo "🔍 Verifying deployment..."

        SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
          --region=${{ env.REGION }} \
          --project=${{ env.PROJECT_ID }} \
          --format="value(status.url)")

        echo "🌐 Service URL: ${SERVICE_URL}"

        # Health check with retry
        for i in {1..5}; do
          echo "Health check attempt $i..."
          if curl -sf "${SERVICE_URL}/health" > /dev/null; then
            echo "✅ Health check passed!"
            break
          fi
          if [ $i -eq 5 ]; then
            echo "❌ Health check failed after 5 attempts"
            exit 1
          fi
          sleep 10
        done

        echo ""
        echo "=========================================="
        echo "✅ API Server Deployed Successfully!"
        echo "=========================================="
        echo "🌐 URL: ${SERVICE_URL}"
        echo "🏥 Health: ${SERVICE_URL}/health"
        echo "📅 Time: $(date)"
        echo "📝 Commit: ${{ github.sha }}"

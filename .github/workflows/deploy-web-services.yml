name: Deploy Web Services (Cloud Run)

# Deploy web services to Cloud Run on push to main/develop
# H9-0: Docker(Container) 배포 방식으로 전환
# 변경된 웹서버만 배포됨

concurrency:
  group: deploy-web-${{ github.ref }}
  cancel-in-progress: false

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'services/web-neture/**'
      - 'services/web-k-cosmetics/**'
      - 'services/web-kpa-society/**'
      - 'services/web-pharmacy-hub/**'
      - 'services/web-lecture/**'
      # WO-O4O-UNIFIED-STORE-WORKSPACE-FOUNDATION-V1 — store.neture.co.kr (Unified Store Workspace)
      - 'services/web-store/**'
      - 'services/web-kpa-branch/**'
      # WO-O4O-SIGNAGE-PLAYER-WEB-DEPLOYMENT-ADOPTION-AND-PRODUCTION-SMOKE-V1
      - 'services/signage-player-web/**'
      # WO-O4O-HOSPITAL-PHARMACY-SERVICE-FOUNDATION-V1 — neture.co.kr/hospital (병원약국 · 임시 서비스 · 서브디렉토리)
      - 'services/web-hospital-pharmacy/**'
      - 'packages/**'
      # WO-O4O-WEB-SERVICES-CD-DEPENDENCY-AFFECTED-DEPLOY-GATE-V1 §17:
      # 9개 Dockerfile 이 전부 root 에서 COPY 하는 build 입력이다.
      # trigger 에 없어서 lockfile·root manifest 만 바뀜 push 가 조용히 누락됐다
      # (silent false-negative). 판정은 detector 가 서비스별로 좌혀준다.
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.base.json'
      - 'tsconfig.packages.json'
      - 'scripts/ci/detect-affected.mjs'
      - '.github/workflows/deploy-web-services.yml'
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to deploy (all, neture, k-cosmetics, kpa-society, pharmacy-hub, lecture, store, kpa-branch, signage-player, hospital-pharmacy)'
        required: false
        default: 'all'
      # WO-O4O-WEB-SERVICES-CD-DEPENDENCY-AFFECTED-DEPLOY-GATE-V1 §24 — 판정 재현 전용.
      # 둘 다 주면 service 입력 대신 그 범위를 detector 로 판정한다
      # (API CD gate 와 동일한 선례). 비워두면 기존 수동 배포 동작 그대로다.
      base_sha:
        description: '(optional) 판정 재현 base SHA'
        required: false
      head_sha:
        description: '(optional) 판정 재현 head SHA'
        required: false

env:
  PROJECT_ID: netureyoutube
  REGION: asia-northeast3
  # Default API URL (neture uses this)
  VITE_API_BASE_URL: https://api.neture.co.kr
  # Service-specific API URLs (same-origin for cookie auth)
  VITE_API_URL_KPA_SOCIETY: https://api.neture.co.kr
  VITE_API_URL_K_COSMETICS: https://api.neture.co.kr
  VITE_KPA_URL: https://kpa-society-web-3e3aws7zqa-du.a.run.app
  # WO-O4O-DOMAIN-SSOT-CANONICALIZATION-V1: per-service canonical URL injected as build-arg.
  # Values mirror apps/api-server/src/config/service-catalog.ts O4O_SERVICES[].domain.
  VITE_SERVICE_URL_NETURE: https://neture.co.kr
  VITE_SERVICE_URL_KPA_SOCIETY: https://kpa-society.co.kr
  VITE_SERVICE_URL_K_COSMETICS: https://k-cosmetics.site
  # WO-PHARMACY-HUB-DEPLOY-BOOTSTRAP-AND-MEMBERSHIP-E2E-V1
  VITE_API_URL_PHARMACY_HUB: https://api.neture.co.kr
  VITE_SERVICE_URL_PHARMACY_HUB: https://pharmacyhub.co.kr
  VITE_API_URL_LECTURE: https://api.neture.co.kr
  VITE_SERVICE_URL_LECTURE: https://study.neture.co.kr
  VITE_API_URL_STORE: https://api.neture.co.kr
  VITE_SERVICE_URL_STORE: https://store.neture.co.kr
  # WO-O4O-UNIFIED-STORE-WORKSPACE-FOUNDATION-V1 §8-4: KPA·KCos·PH 매장 진입 → store.neture.co.kr handoff.
  # cutover 전환 = 이 한 줄을 'true' 로 (DNS·cert·Google origin 준비 후). 기본 'false' = 기존 매장 화면 유지.
  VITE_UNIFIED_STORE_HANDOFF: 'false'
  # WO-O4O-KPA-BRANCH-DEPLOY-AND-RUNTIME-SMOKE-V1
  # 분회 서비스는 단일 web service 하나만 운영한다 (분회별 Cloud Run 서비스 생성 금지).
  # 분회 tenant 는 URL segment(/{slug}) 또는 Host(branch_domains) 로만 해석된다.
  VITE_API_URL_KPA_BRANCH: https://api.neture.co.kr
  # WO-O4O-SIGNAGE-PLAYER-WEB-DEPLOYMENT-ADOPTION-AND-PRODUCTION-SMOKE-V1
  # Signage Player 는 익명 단말 런타임이다. public runtime config(API base) 만 주입한다.
  # secret / service account key / DB credential 은 절대 주입하지 않는다.
  VITE_API_URL_SIGNAGE_PLAYER: https://api.neture.co.kr
  # WO-O4O-HOSPITAL-PHARMACY-SERVICE-FOUNDATION-V1
  # 병원약국 전문 서비스. 원내 목록은 브라우저 localStorage 에만 저장(D2) — API base 만 주입한다.
  # DNS·인증서(hospital.neture.co.kr) 는 Foundation 범위 밖(사용자측 · signage-player·pharmacy-hub 선례).
  VITE_API_URL_HOSPITAL_PHARMACY: https://api.neture.co.kr
  VITE_SERVICE_URL_HOSPITAL_PHARMACY: https://neture.co.kr/hospital

jobs:
  deploy-hold-notice:
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   배포 job 이 skip 되면 run 이 조용히 성공처럼 보여 "왜 반영이 안 되나" 착시가 생긴다.
    #   이 job 은 게이트가 닫혔을 때**만** 돌며, 보류 사유를 Job Summary 에 남긴다.
    #   배포 job 과 의존관계가 없으므로 skip 여부와 무관하게 실행된다.
    if: vars.DEPLOY_ENABLED != 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Report deploy hold
        run: |
          {
            echo "## ⛔ 배포 보류 — DEPLOY_ENABLED ≠ 'true'"
            echo ""
            echo "이 워크플로의 배포 job 은 실행되지 않았습니다 (build · migration · Cloud Run deploy 0)."
            echo ""
            echo "- 현재 값: '${{ vars.DEPLOY_ENABLED }}'  (변수 부재 · 공백도 차단 = fail-closed)"
            echo "- 사유: WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 INCIDENT — main 의 Lecture Phase 2 runtime 이 data cutover 전에 서빙되는 것을 막는다."
            echo "- 해제: 사용자 승인 후 \`gh variable set DEPLOY_ENABLED --body true\`"
            echo "- commit: ${{ github.sha }}"
          } >> "$GITHUB_STEP_SUMMARY"
          echo "DEPLOY_HOLD_ACTIVE=1 (DEPLOY_ENABLED='${{ vars.DEPLOY_ENABLED }}')"

  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      neture: ${{ steps.changes.outputs.neture }}
      k-cosmetics: ${{ steps.changes.outputs.k-cosmetics }}
      kpa-society: ${{ steps.changes.outputs.kpa-society }}
      pharmacy-hub: ${{ steps.changes.outputs.pharmacy-hub }}
      lecture: ${{ steps.changes.outputs.lecture }}
      store: ${{ steps.changes.outputs.store }}
      kpa-branch: ${{ steps.changes.outputs.kpa-branch }}
      signage-player: ${{ steps.changes.outputs.signage-player }}
      hospital-pharmacy: ${{ steps.changes.outputs.hospital-pharmacy }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          # WO-O4O-WEB-DEPLOY-DETECT-CHANGES-LAST-DEPLOYED-SHA-V1:
          # full history needed to diff github.event.before..github.sha (multi-commit push batch)
          fetch-depth: 0

      - name: Detect changed services
        id: changes
        run: |
          # 판정 재현 모드 — base/head 를 명시하면 detector 를 그대로 돌린다 (§24).
          if [ -n "${{ github.event.inputs.base_sha }}" ] && [ -n "${{ github.event.inputs.head_sha }}" ]; then
            echo "replay mode: ${{ github.event.inputs.base_sha }}..${{ github.event.inputs.head_sha }}"
            BASE_SHA="${{ github.event.inputs.base_sha }}" HEAD_SHA="${{ github.event.inputs.head_sha }}"               node scripts/ci/detect-affected.mjs
            exit 0
          fi

          # For workflow_dispatch with specific service
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            SERVICE="${{ github.event.inputs.service }}"
            if [ "$SERVICE" = "all" ]; then
              echo "neture=true" >> $GITHUB_OUTPUT
              echo "k-cosmetics=true" >> $GITHUB_OUTPUT
              echo "kpa-society=true" >> $GITHUB_OUTPUT
              echo "pharmacy-hub=true" >> $GITHUB_OUTPUT
              echo "lecture=true" >> $GITHUB_OUTPUT
              echo "store=true" >> $GITHUB_OUTPUT
              echo "kpa-branch=true" >> $GITHUB_OUTPUT
              echo "signage-player=true" >> $GITHUB_OUTPUT
              echo "hospital-pharmacy=true" >> $GITHUB_OUTPUT
            else
              echo "neture=$( [ "$SERVICE" = 'neture' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "k-cosmetics=$( [ "$SERVICE" = 'k-cosmetics' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "kpa-society=$( [ "$SERVICE" = 'kpa-society' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "pharmacy-hub=$( [ "$SERVICE" = 'pharmacy-hub' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "lecture=$( [ "$SERVICE" = 'lecture' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "store=$( [ "$SERVICE" = 'store' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "kpa-branch=$( [ "$SERVICE" = 'kpa-branch' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "signage-player=$( [ "$SERVICE" = 'signage-player' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
              echo "hospital-pharmacy=$( [ "$SERVICE" = 'hospital-pharmacy' ] && echo 'true' || echo 'false' )" >> $GITHUB_OUTPUT
            fi
            exit 0
          fi

          # WO-O4O-WEB-DEPLOY-DETECT-CHANGES-LAST-DEPLOYED-SHA-V1:
          # push events — compare the WHOLE pushed batch (event.before..github.sha) instead of
          # tip-only (HEAD~1 HEAD). tip-only skipped web deploys when a docs-only/other commit
          # became HEAD after the web-change commit in the same push.
          HEAD_SHA="${{ github.sha }}"
          BASE_SHA="${{ github.event.before }}"

          # fallback when before is unusable: all-zeros (new branch / first push / force push)
          # or not present locally → use HEAD~1; if even that is missing, the detector's own
          # safe fallback (base SHA 없음 → 전 서비스 true) takes over.
          if [ -z "$BASE_SHA" ] || [ "$BASE_SHA" = "0000000000000000000000000000000000000000" ] || ! git cat-file -e "${BASE_SHA}^{commit}" 2>/dev/null; then
            echo "event.before unusable ('${BASE_SHA}') — fallback to HEAD~1"
            BASE_SHA=$(git rev-parse HEAD~1 2>/dev/null || echo "")
          fi

          echo "event=push"
          echo "base_sha=${BASE_SHA:-<none>}"
          echo "head_sha=${HEAD_SHA}"

          # WO-O4O-WEB-SERVICES-CD-DEPENDENCY-AFFECTED-DEPLOY-GATE-V1 §6 · §7 · §15
          #
          # 이전 정책은 `packages/** 변경 → 9개 서비스 전부 재배포` 였다.
          # 실측상 `hospital-pharmacy-core` 한 package 변경으로 9개가 전부 배포됐고
          # 그중 8개는 그 package 를 dependency closure 안에 갖고 있지도 않았다.
          #
          # 판정은 Admin·Docs·API 와 같은 SSOT(`scripts/ci/detect-affected.mjs`) 다.
          # workspace dependency graph 의 transitive closure 만 쓴다 —
          # package 이름 allowlist/denylist 는 어느 쪽에도 없다.
          #
          # output key 는 기존과 동일하다 — detector 가 그 이름 그대로 써넣는다.
          BASE_SHA="$BASE_SHA" HEAD_SHA="$HEAD_SHA" node scripts/ci/detect-affected.mjs

  deploy-neture:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.neture == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet

      - name: Build and push Docker image
        run: |
          echo "Building neture-web Docker image..."
          IMAGE_TAG="${{ github.sha }}"

          # Build from repo root with Dockerfile path (for workspace packages)
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_BASE_URL }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_NETURE }} \
            -f services/web-neture/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/neture-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/neture-web:latest \
            .

          docker push gcr.io/${{ env.PROJECT_ID }}/neture-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/neture-web:latest

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying neture-web to Cloud Run..."

          gcloud run deploy neture-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/neture-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5

          echo "neture-web deployed successfully!"

      - name: Verify deployment
        run: |
          SERVICE_URL=$(gcloud run services describe neture-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)")

          echo "Service URL: ${SERVICE_URL}"

          if curl -sf "${SERVICE_URL}" > /dev/null; then
            echo "neture-web is accessible!"
          else
            echo "neture-web may need a moment to start"
          fi

  deploy-k-cosmetics:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.k-cosmetics == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet

      - name: Build and push Docker image
        run: |
          echo "Building k-cosmetics-web Docker image..."
          IMAGE_TAG="${{ github.sha }}"

          # Build from repo root with Dockerfile path (for workspace packages)
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_K_COSMETICS }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_K_COSMETICS }} \
            --build-arg VITE_UNIFIED_STORE_HANDOFF=${{ env.VITE_UNIFIED_STORE_HANDOFF }} \
            -f services/web-k-cosmetics/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/k-cosmetics-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/k-cosmetics-web:latest \
            .

          docker push gcr.io/${{ env.PROJECT_ID }}/k-cosmetics-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/k-cosmetics-web:latest

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying k-cosmetics-web to Cloud Run..."

          gcloud run deploy k-cosmetics-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/k-cosmetics-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5

          echo "k-cosmetics-web deployed successfully!"

      - name: Verify deployment
        run: |
          SERVICE_URL=$(gcloud run services describe k-cosmetics-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)")

          echo "Service URL: ${SERVICE_URL}"

  deploy-kpa-society:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.kpa-society == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet

      - name: Build and push Docker image
        run: |
          echo "Building kpa-society-web Docker image..."
          IMAGE_TAG="${{ github.sha }}"

          # Build from repo root with Dockerfile path (for workspace packages)
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_KPA_SOCIETY }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_KPA_SOCIETY }} \
            --build-arg VITE_UNIFIED_STORE_HANDOFF=${{ env.VITE_UNIFIED_STORE_HANDOFF }} \
            -f services/web-kpa-society/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/kpa-society-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/kpa-society-web:latest \
            .

          docker push gcr.io/${{ env.PROJECT_ID }}/kpa-society-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/kpa-society-web:latest

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying kpa-society-web to Cloud Run..."

          gcloud run deploy kpa-society-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/kpa-society-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5

          echo "kpa-society-web deployed successfully!"

      - name: Verify deployment
        run: |
          SERVICE_URL=$(gcloud run services describe kpa-society-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)")

          echo "Service URL: ${SERVICE_URL}"

  # WO-PHARMACY-HUB-DEPLOY-BOOTSTRAP-AND-MEMBERSHIP-E2E-V1
  #   기존 4개 서비스 job 과 동일 구조. DNS(pharmacyhub.co.kr) 매핑은 이 WO 범위 밖 —
  #   임시 Cloud Run URL 로 가입·승인 E2E 검증까지만 수행한다.
  deploy-pharmacy-hub:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.pharmacy-hub == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet

      - name: Build and push Docker image
        run: |
          echo "Building pharmacy-hub-web Docker image..."
          IMAGE_TAG="${{ github.sha }}"

          # Build from repo root with Dockerfile path (for workspace packages)
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_PHARMACY_HUB }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_PHARMACY_HUB }} \
            --build-arg VITE_UNIFIED_STORE_HANDOFF=${{ env.VITE_UNIFIED_STORE_HANDOFF }} \
            -f services/web-pharmacy-hub/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/pharmacy-hub-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/pharmacy-hub-web:latest \
            .

          docker push gcr.io/${{ env.PROJECT_ID }}/pharmacy-hub-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/pharmacy-hub-web:latest

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying pharmacy-hub-web to Cloud Run..."

          gcloud run deploy pharmacy-hub-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/pharmacy-hub-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5

          echo "pharmacy-hub-web deployed successfully!"

      - name: Verify deployment
        run: |
          SERVICE_URL=$(gcloud run services describe pharmacy-hub-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)")

          echo "Service URL: ${SERVICE_URL}"

  deploy-lecture:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.lecture == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet
      - name: Build and push Docker image
        run: |
          IMAGE_TAG="${{ github.sha }}"
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_LECTURE }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_LECTURE }} \
            -f services/web-lecture/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/lecture-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/lecture-web:latest \
            .
          docker push gcr.io/${{ env.PROJECT_ID }}/lecture-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/lecture-web:latest
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy lecture-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/lecture-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5
      - name: Verify deployment
        run: |
          gcloud run services describe lecture-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)"

  deploy-hospital-pharmacy:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.hospital-pharmacy == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet
      - name: Build and push Docker image
        run: |
          IMAGE_TAG="${{ github.sha }}"
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_HOSPITAL_PHARMACY }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_HOSPITAL_PHARMACY }} \
            -f services/web-hospital-pharmacy/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/hospital-pharmacy-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/hospital-pharmacy-web:latest \
            .
          docker push gcr.io/${{ env.PROJECT_ID }}/hospital-pharmacy-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/hospital-pharmacy-web:latest
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy hospital-pharmacy-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/hospital-pharmacy-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5
      - name: Verify deployment
        run: |
          gcloud run services describe hospital-pharmacy-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)"

  deploy-store:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.store == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}
      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet
      - name: Build and push Docker image
        run: |
          IMAGE_TAG="${{ github.sha }}"
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_STORE }} \
            --build-arg VITE_SERVICE_URL=${{ env.VITE_SERVICE_URL_STORE }} \
            -f services/web-store/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/store-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/store-web:latest \
            .
          docker push gcr.io/${{ env.PROJECT_ID }}/store-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/store-web:latest
      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy store-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/store-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5
      - name: Verify deployment
        run: |
          gcloud run services describe store-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)"

  deploy-kpa-branch:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.kpa-branch == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet

      - name: Build and push Docker image
        run: |
          echo "Building kpa-branch-web Docker image..."
          IMAGE_TAG="${{ github.sha }}"

          # Build from repo root with Dockerfile path (for workspace packages)
          docker build             --platform linux/amd64             --build-arg VITE_API_BASE_URL=${{ env.VITE_API_URL_KPA_BRANCH }}             -f services/web-kpa-branch/Dockerfile             -t gcr.io/${{ env.PROJECT_ID }}/kpa-branch-web:${IMAGE_TAG}             -t gcr.io/${{ env.PROJECT_ID }}/kpa-branch-web:latest             .

          docker push gcr.io/${{ env.PROJECT_ID }}/kpa-branch-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/kpa-branch-web:latest

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying kpa-branch-web to Cloud Run..."

          gcloud run deploy kpa-branch-web             --image=gcr.io/${{ env.PROJECT_ID }}/kpa-branch-web:${{ github.sha }}             --region=${{ env.REGION }}             --project=${{ env.PROJECT_ID }}             --platform=managed             --allow-unauthenticated             --port=8080             --memory=256Mi             --cpu=1             --min-instances=0             --max-instances=5

          echo "kpa-branch-web deployed successfully!"

      - name: Verify deployment
        run: |
          SERVICE_URL=$(gcloud run services describe kpa-branch-web             --region=${{ env.REGION }}             --project=${{ env.PROJECT_ID }}             --format="value(status.url)")

          echo "Service URL: ${SERVICE_URL}"

  # WO-O4O-SIGNAGE-PLAYER-WEB-DEPLOYMENT-ADOPTION-AND-PRODUCTION-SMOKE-V1
  # 판정: A. INDEPENDENT_WEB_SERVICE.
  # signage-player-web 은 자체 SPA router(/signage/... · /player/...) 와 SPA fallback 이
  # 필요한 독립 런타임이며, 기존 web service 중 이 route 를 담당하는 서비스가 없다.
  # 신규 인프라를 설계하지 않고 이 workflow 의 기존 per-service 패턴만 그대로 재사용한다.
  # 접근 주소는 Cloud Run 생성 URL 을 쓴다 (hostname/DNS 정책 변경 없음).
  deploy-signage-player:
    needs: detect-changes
    # WO-O4O-LECTURE-INDEPENDENT-SERVICE-SEPARATION-V1 §19 (2026-09-23 INCIDENT)
    #   DEPLOY GATE (fail-closed): `DEPLOY_ENABLED` 가 'true' 일 때만 배포한다.
    #   변수가 없거나 비어 있으면 Actions 가 빈 문자열을 돌려주므로 **차단이 기본값**이다.
    if: vars.DEPLOY_ENABLED == 'true' && needs.detect-changes.outputs.signage-player == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for GCR
        run: gcloud auth configure-docker gcr.io --quiet

      - name: Build and push Docker image
        run: |
          echo "Building signage-player-web Docker image..."
          IMAGE_TAG="${{ github.sha }}"

          # Build from repo root with Dockerfile path (for workspace packages)
          docker build \
            --platform linux/amd64 \
            --build-arg VITE_API_URL=${{ env.VITE_API_URL_SIGNAGE_PLAYER }} \
            -f services/signage-player-web/Dockerfile \
            -t gcr.io/${{ env.PROJECT_ID }}/signage-player-web:${IMAGE_TAG} \
            -t gcr.io/${{ env.PROJECT_ID }}/signage-player-web:latest \
            .

          docker push gcr.io/${{ env.PROJECT_ID }}/signage-player-web:${IMAGE_TAG}
          docker push gcr.io/${{ env.PROJECT_ID }}/signage-player-web:latest

      - name: Deploy to Cloud Run
        run: |
          echo "Deploying signage-player-web to Cloud Run..."

          gcloud run deploy signage-player-web \
            --image=gcr.io/${{ env.PROJECT_ID }}/signage-player-web:${{ github.sha }} \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --memory=256Mi \
            --cpu=1 \
            --min-instances=0 \
            --max-instances=5

          echo "signage-player-web deployed successfully!"

      - name: Verify deployment
        run: |
          SERVICE_URL=$(gcloud run services describe signage-player-web \
            --region=${{ env.REGION }} \
            --project=${{ env.PROJECT_ID }} \
            --format="value(status.url)")

          echo "Service URL: ${SERVICE_URL}"

          # nginx /health (SPA fallback 이 아닌 전용 endpoint)
          if curl -sf "${SERVICE_URL}/health" > /dev/null; then
            echo "signage-player-web /health OK"
          else
            echo "signage-player-web may need a moment to start"
          fi

  summary:
    # WO-O4O-WEB-SERVICES-CD-DEPENDENCY-AFFECTED-DEPLOY-GATE-V1 §21:
    # lecture·store·hospital-pharmacy 가 뒤달아 추가될 때 needs 와 출력 목록이
    # 함께 갱신되지 않았다 — 그 3개는 summary 에 전혀 드러나지 않았다.
    needs:
      - detect-changes
      - deploy-neture
      - deploy-k-cosmetics
      - deploy-kpa-society
      - deploy-pharmacy-hub
      - deploy-lecture
      - deploy-store
      - deploy-kpa-branch
      - deploy-signage-player
      - deploy-hospital-pharmacy
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Summary
        run: |
          echo "=========================================="
          echo "Web Services Deployment Summary"
          echo "=========================================="
          echo ""
          echo "Services deployed:"
          echo "  - neture-web: ${{ needs.detect-changes.outputs.neture }}"
          echo "  - k-cosmetics-web: ${{ needs.detect-changes.outputs.k-cosmetics }}"
          echo "  - kpa-society-web: ${{ needs.detect-changes.outputs.kpa-society }}"
          echo "  - pharmacy-hub-web: ${{ needs.detect-changes.outputs.pharmacy-hub }}"
          echo "  - lecture-web: ${{ needs.detect-changes.outputs.lecture }}"
          echo "  - store-web: ${{ needs.detect-changes.outputs.store }}"
          echo "  - kpa-branch-web: ${{ needs.detect-changes.outputs.kpa-branch }}"
          echo "  - signage-player-web: ${{ needs.detect-changes.outputs.signage-player }}"
          echo "  - hospital-pharmacy-web: ${{ needs.detect-changes.outputs.hospital-pharmacy }}"
          echo ""
          echo "Time: $(date)"
          echo "Commit: ${{ github.sha }}"
          echo "Deployment: Docker (Container)"
          echo "=========================================="